# 어떤 모델을, 어떻게, 왜 파인튜닝하는가

이 문서는 사투리→표준어 음성 번역 프로젝트에서 **무엇을 어떤 방식으로 파인튜닝하며 왜 그렇게 결정했는지**를 정리한다. 면접·포트폴리오에서 설계 근거를 설명하기 위한 것이며, 뒷부분에는 더 깊게 파고들 만한 주제를 따로 모았다. 진행 기록은 [DEVELOPMENT_JOURNEY.md](DEVELOPMENT_JOURNEY.md), 구현은 [data/](data/) · [notebooks/](notebooks/) · [backend/](backend/) 참고.

---

## 0. 전체 그림 — 왜 모델이 둘인가

```
음성 ──[① STT: Whisper]──▶ 사투리 텍스트 ──[② 변환: KoBART]──▶ 표준어 텍스트
```

파이프라인은 성격이 다른 두 문제로 나뉜다.

| 단계 | 문제 | 입력 → 출력 | 모델 |
|------|------|-------------|------|
| ① STT | 사투리 발음/억양을 못 알아듣는 문제 | 음성 → 사투리 텍스트 | Whisper (파인튜닝 예정) |
| ② 변환 | 사투리 어휘/어미를 표준어로 바꾸는 문제 | 사투리 텍스트 → 표준어 텍스트 | KoBART (파인튜닝 진행 중) |

둘 다 **encoder-decoder(seq2seq) Transformer**를 지도학습으로 파인튜닝하지만, 입력 형태(음성 vs 텍스트)와 데이터가 달라 따로 학습한다.

프로젝트의 핵심 서사는 **"표준 모델이 잘 못하는 영역(사투리)의 원인을 데이터에서 찾아 보강·재학습으로 개선한다"** 는 방법론이다.

---

## 1. 변환 모델 — KoBART

### 1-1. 태스크 정의

`사투리 텍스트 → 표준어 텍스트`. 대부분의 어절은 그대로이고 일부만 바뀌는 **입력 의존적 문장 재작성**이다.

```
밥 문나            → 밥 먹었니
가가 억수로 머라카노 → 그 아이가 굉장히 뭐라고 하니
자랄꺼 아이가       → 자랄꺼 아니야
```

### 1-2. 모델 선택: `gogamza/kobart-base-v2` (약 1.2억 파라미터)

- **KoBART = BART 아키텍처의 한국어 버전.** BART는 encoder–decoder Transformer로, 인코더가 입력 문장을 이해하고 디코더가 출력 문장을 토큰 하나씩 생성한다.
- BART의 사전학습은 **denoising**(문장을 일부러 망가뜨린 뒤 원문 복원)이라, "입력을 조금 고쳐 올바른 문장으로 만드는" 우리 태스크와 목적이 정확히 맞닿아 있다.

**왜 이 선택인가 (대안 대비)**

1. **왜 encoder-decoder(seq2seq)인가 — GPT류(decoder-only) 대비**
   사투리↔표준어는 입력과 출력이 대부분 겹치는 재작성이다. 입력 전체를 양방향으로 보는 인코더가 있어야 "어디를 바꾸고 어디를 유지할지"를 안정적으로 판단한다. decoder-only는 프롬프트로도 가능하지만 이런 정렬된 재작성에는 seq2seq가 더 자연스럽고 데이터 효율이 좋다.

2. **왜 한국어 전용 사전학습(KoBART)인가 — 다국어/영어 모델 대비**
   한국어 토크나이저·어휘·문법을 이미 학습한 상태라, 우리 데이터(45만 쌍)만으로 빠르게 수렴한다. 영어 기반 모델은 한국어를 처음부터 배워야 해 비효율적이다.

3. **왜 작은 모델(base)인가 — 대형 LLM 대비**
   태스크가 좁아(문장 정규화) 1.2억 파라미터로 충분하다. 작아서 **무료 GPU(Colab)에서 전체 파인튜닝**이 가능하고 **서버 추론 비용도 낮다**. 대형 LLM은 과잉이며 배포·비용 부담이 크다.

### 1-3. 파인튜닝 방식

- **학습 형태**: 지도학습 seq2seq 파인튜닝. 입력=사투리 문장, 타깃=표준어 문장.
- **손실함수**: **teacher forcing + cross-entropy**. 디코더가 정답 표준어 토큰을 순서대로 맞추도록(다음 토큰 예측) 최적화한다.
- **범위**: **전체 파라미터 파인튜닝**(LoRA 같은 부분학습 아님). 모델이 작아 전체를 학습해도 부담이 없고, 도메인이 뚜렷해 전체 적응이 유리하다.
- **하이퍼파라미터**: lr `5e-5`, batch `32`, epoch `2`, `fp16`. 추론은 beam search(`num_beams=4`).
- **베이스 모델**: `gogamza/kobart-base-v2`, 프레임워크 HuggingFace `transformers`(4.46.3 고정), `Seq2SeqTrainer`.

구현: [notebooks/kobart_finetune.ipynb](notebooks/kobart_finetune.ipynb).

### 1-4. 데이터를 왜 그렇게 다듬었나

AI Hub 경상도 라벨에서 **209만 쌍**을 뽑았더니 **84%가 사투리=표준(동일 쌍)**이었다. 이는 대부분의 어절이 표준과 같은 방언 데이터의 자연스러운 성질이지만, 그대로 학습하면 모델이 **"그냥 베끼기"만** 배우기 쉽다.

그래서 [data/build_mt_dataset.py](data/build_mt_dataset.py)에서:
1. 완전 중복 쌍 제거
2. 동일 쌍을 **목표 비율(30%)까지 다운샘플링** → 변형이 있는 쌍에 학습 집중
3. 길이 필터 + 셔플

결과: **train 45.5만 쌍(변형 31.9만 + 동일 13.7만), val 5천**.

> 동일 쌍을 0으로 만들지 않는 이유: 실제로 대부분 문장은 그대로 통과시켜야 하므로, "유지"도 배워야 한다. 30%는 "변형 학습"과 "불필요한 변경 억제" 사이의 균형점.

### 1-5. 어떻게 평가하나

- **기준선(copy)**: 아무것도 안 하고 사투리를 그대로 표준어로 내보내는 것.
- **지표**: **CER(글자 오류율)**과 **정확일치율**을 copy vs KoBART로 비교.
- 동일 쌍이 많아 copy도 CER이 낮게 나오므로, **실제 변형 쌍에서 모델이 copy를 이기는지**가 진짜 판단 기준이다. 이 "기준선 대비 개선"이 포트폴리오의 정량 지표다.

### 1-6. 하이퍼파라미터를 왜 이 값으로 정했나

값들은 임의로 고른 게 아니라 **(a) 사전학습 Transformer 파인튜닝의 관례, (b) 하드웨어(무료 Colab T4 ≈ 16GB) 제약, (c) 우리 데이터의 성질(짧은 대화 문장, 45만 쌍)** 세 가지로 정한 "합리적 출발점"이다. 실제로는 val 지표를 보며 조정한다.

| 요소 | 값 | 근거 | 조정 방향 |
|------|----|------|-----------|
| batch size | 32 | T4 메모리에 128토큰×1.2억 모델을 fp16으로 담기 좋은 크기. 배치가 클수록 그래디언트가 안정적·학습이 빠름 | OOM이면 16으로 낮추고 `gradient_accumulation_steps`로 유효 배치 유지 |
| learning rate | 5e-5 | 사전학습 가중치를 **크게 흔들지 않는** 파인튜닝 표준 범위(1e-5~5e-5) 상단. 도메인 적응엔 충분히 크되 사전지식은 보존 | 손실이 튀면 3e-5/2e-5로, 수렴이 너무 느리면 소폭 ↑ |
| epochs | 2 | 45만 쌍이면 1~3에폭에 대개 수렴. 과하면 과적합 | val 손실이 오르기 시작하면 조기 종료. 빠른 실험은 1 |
| max_length | 128 토큰 | 대화 발화가 짧아 대부분 128 이내. 어텐션 비용이 길이 제곱(O(n²))이라 불필요하게 키우면 메모리·속도 손해 | 길이 분포 확인 후 커버율 높은 값으로(잘림 많으면 ↑) |
| fp16 | GPU면 on | mixed precision으로 메모리 절반·속도 향상, 정확도 손실 거의 없음 | T4는 fp16, A100 등은 bf16이 더 안정적 |
| weight_decay | 0.01 | L2 규제로 과적합 완화 — Transformer 파인튜닝 표준값 | 과적합 심하면 ↑ |
| warmup | (제거) | 원래 `warmup_ratio=0.05`로 초반 lr을 0→목표로 서서히 올려 초기 불안정을 줄이려 했으나 transformers 버전 호환으로 제외. 안정성이 필요하면 `warmup_steps`로 대체 | 초반 손실 발산 시 warmup 재도입 |
| num_beams (추론) | 4 | greedy(1)보다 문장 전체 확률이 높은 결과. 8+는 이득 대비 느림 | 속도 급하면 1~2, 품질 급하면 ↑ |
| save_strategy / save_total_limit | epoch / 1 | 에폭마다 저장하되 최신 1개만 — Drive 용량 절약 | 체크포인트 비교가 필요하면 ↑ |
| optimizer | AdamW(기본) | Transformer 파인튜닝 사실상 표준. Trainer 기본값 사용 | 보통 그대로 |

**값을 데이터·하드웨어로부터 역산하는 사고 흐름**
- `max_length` ← 데이터 길이 분포(대부분 짧음)에서 커버율로 결정.
- `batch size` ← GPU 메모리에서 역산(모델·시퀀스·정밀도가 정해지면 상한이 나옴).
- `learning rate`·`epochs` ← **정답이 없어 val 곡선을 보며 조정**하는 대상. 그래서 위 값은 "탐색의 출발점"이지 고정 상수가 아니다.
- 핵심 원칙: **사전학습 지식을 보존하면서(작은 lr) 도메인에 적응(충분한 데이터·에폭)** 하고, **하드웨어 한계 안에서(batch·fp16·max_length)** 돌리는 것.

> STT(Whisper)도 같은 원리다. 다만 입력이 30초 log-mel이라 시퀀스가 길어 batch를 더 작게(예: 8~16) 잡고, 데이터가 적으면 인코더 하위층을 얼려 과적합을 줄이는 선택이 추가된다.

---

## 2. STT 모델 — Whisper (예정)

### 2-1. 태스크와 모델

`사투리 음성 → 사투리 텍스트`. 모델은 OpenAI **Whisper base(74M)/small(244M)**. Whisper도 encoder-decoder Transformer지만, 인코더 입력이 텍스트가 아니라 **음성의 log-mel 스펙트로그램**이고 디코더가 텍스트를 생성한다.

### 2-2. 왜 파인튜닝하나 (핵심 서사)

표준 Whisper는 **표준어 발음 위주**로 학습돼 경상도 억양·발음(`머라카노`, `문나`)에서 인식률이 떨어진다. **사투리 음성으로 파인튜닝**해 이 격차를 줄이는 것이 곧 "표준 모델이 못하는 영역을 데이터로 개선"이다.

### 2-3. 방식과 평가

- 음성 클립 → **사투리 전사**를 타깃으로 cross-entropy 파인튜닝(파이프라인이 2단계라 STT는 사투리 텍스트까지만 책임진다).
- 평가: **표준 Whisper baseline vs 파인튜닝 모델의 CER/WER** 개선폭.
- 서빙: 파인튜닝 모델을 CTranslate2로 변환해 백엔드 `WHISPER_MODEL_DIR`에 연결(현재 추론 엔진 `faster-whisper`).

---

## 3. 왜 2단계로 나눴나 (설계 근거)

**대안**: Whisper 하나로 `사투리 음성 → 표준어 텍스트`를 직접 파인튜닝(1단계 통합).

**2단계를 택한 이유**
1. **역할 분리** — STT는 "들리는 대로(사투리)", 변환은 "표준어로". 각 단계의 데이터·평가·개선이 명확해진다.
2. **독립 진행 가능** — 변환 모델은 음성 없이 텍스트만으로 학습 가능. 실제로 음성(28GB) 확보가 막힌 동안 변환 트랙을 먼저 끝낼 수 있었다.
3. **디버깅 용이** — 어느 단계가 틀렸는지 분리 관찰이 된다.

**트레이드오프(솔직하게)**
- **에러 전파**: STT가 틀리면 변환도 영향받는다.
- **모델 2개 유지 비용**.
- → "완성도를 높이면 1단계 통합(음성→표준어 직접)도 실험 가능"이 자연스러운 확장 스토리.

---

## 4. 더 깊게 파고들 주제 (deep dives)

### 4-1. BART의 denoising 사전학습이 왜 '교정' 태스크에 맞나
BART는 원문을 **텍스트 인필링(span 마스킹)·문장 순서 뒤섞기** 등으로 망가뜨린 뒤 디코더로 원문을 복원하도록 사전학습된다. 즉 "손상된 입력 → 올바른 문장"을 이미 대규모로 연습한 모델이다. 사투리→표준어도 "규범에서 벗어난 입력 → 규범 문장"이라 사전학습과 파인튜닝의 목적이 일치해, 적은 데이터로도 잘 적응한다.

### 4-2. teacher forcing과 cross-entropy
학습 때 디코더는 이전 위치의 **정답 토큰**을 입력으로 받아 다음 토큰을 예측한다(teacher forcing). 각 위치에서 정답 토큰의 확률을 높이는 방향으로 cross-entropy를 최소화한다. 장점은 학습 안정·병렬화, 단점은 학습(정답 주입)과 추론(자기 예측 주입)의 분포 차이(exposure bias) — beam search와 충분한 데이터로 완화한다.

### 4-3. beam search vs greedy (`num_beams`)
추론은 토큰을 하나씩 고르는데, greedy는 매 순간 최고 확률 하나만 택한다. beam search는 상위 k개 후보(beam)를 동시에 확장해 문장 전체 확률이 높은 결과를 고른다. `num_beams=4`는 품질↑·속도↓의 절충. 재작성 태스크는 한 글자 차이가 의미를 바꾸므로 beam이 유리하다.

### 4-4. 왜 CER인가 (WER 대비)
- **CER**: 글자 단위 편집거리 ÷ 정답 글자 수. **WER**: 단어(어절) 단위.
- 한국어는 띄어쓰기·어절 경계가 모호해 WER이 과하게 나빠질 수 있어, **글자 단위 CER이 더 안정적**이다. 두 지표를 함께 보되 주지표는 CER로 둔다.

### 4-5. 전체 파인튜닝 vs LoRA/PEFT
- **LoRA**는 원 가중치를 얼리고 저랭크 어댑터만 학습해 메모리·저장을 크게 줄인다(대형 모델에 유용).
- 지금은 모델이 작아(1.2억) 전체 파인튜닝이 GPU에 무리 없고, 단일 도메인 적응에는 전체 학습이 대체로 더 좋은 품질을 낸다. → **지금은 full FT**, 이후 모델을 키우거나 여러 지역을 한 모델에 얹을 때 LoRA를 검토.

### 4-6. 동일 쌍 비율(positive 비중) 튜닝
동일 쌍 비율이 너무 높으면 "복사"만, 너무 낮으면 멀쩡한 문장도 과하게 바꾸는 **과교정(over-correction)**이 생긴다. 30%는 시작값이며, val에서 과교정(정답이 동일인데 바꿔버림)과 미교정(변형을 놓침)을 함께 보고 조정한다.

### 4-7. Whisper 파인튜닝 디테일
- 입력은 30초 윈도의 log-mel 스펙트로그램. 디코더는 언어·태스크 특수 토큰으로 시작한다(한국어·transcribe).
- 오디오 인코더(하위 특징 추출)를 **얼리고** 상위/디코더만 학습하면 적은 데이터에서 과적합을 줄일 수 있다(데이터량에 따라 선택).
- 무음 구간 환각을 줄이기 위해 VAD·짧은/긴 발화 필터를 전처리에서 이미 적용([data/preprocess.py](data/preprocess.py)).

### 4-8. 서빙 최적화 — CTranslate2·양자화
파인튜닝한 Whisper를 CTranslate2로 변환하고 **int8 양자화**하면 CPU 추론이 빨라지고 메모리가 준다. 서버 비용/지연을 낮추는 실전 포인트이며, 이후 온디바이스(ONNX/TFLite) 확장의 발판이 된다.

### 4-9. 에러 전파 완화
STT 오류가 변환으로 넘어가는 문제는 (a) STT 파인튜닝으로 상류 오류 자체를 줄이고, (b) 변환 모델을 **약간 노이즈가 있는 입력**에도 견디게 학습(STT가 흔히 내는 오인식을 증강)하는 방향으로 줄일 수 있다.

### 4-10. 데이터 품질·편향
AI Hub 라벨 메타에는 지역·화자·주제(topic) 정보가 있다. 특정 주제·화자에 치우치면 일반화가 나빠지므로, 세션 단위 분할(누수 방지)에 더해 주제/화자 분포를 확인하고 필요하면 균형을 맞춘다. 경상도로 시작하되 이후 전라·충청 등으로 확장할 때 지역 간 균형도 같은 방식으로 관리한다.

---

## 5. 한눈 요약

| 항목 | 변환(KoBART) | STT(Whisper) |
|------|--------------|--------------|
| 입력 → 출력 | 사투리 텍스트 → 표준어 텍스트 | 사투리 음성 → 사투리 텍스트 |
| 구조 | encoder-decoder | encoder-decoder(음성 입력) |
| 크기 | ~1.2억 | 74M/244M |
| 데이터 | 45.5만 문장쌍(균형) | 발화 클립 + 전사 |
| 학습 | full FT, teacher forcing + CE | 파인튜닝(인코더 일부 freeze 옵션) |
| 평가 | copy 대비 CER·정확일치 | 표준 Whisper 대비 CER/WER |
| 상태 | 진행 중(노트북·백엔드 연동) | 예정(음성 확보 후) |

**핵심 한 줄**: 태스크 구조(문장→문장, 음성→문장)에 맞는 **작고 한국어에 특화된 encoder-decoder**를 골라 지도학습으로 파인튜닝하고, **"안 한 것 대비 얼마나 좋아졌나(CER)"**로 개선을 증명한다.
