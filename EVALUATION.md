# STT 정량 평가 — 사투리 Whisper (4지역 LoRA)

표준 Whisper가 약한 사투리 인식을, 방언 음성으로 파인튜닝해 얼마나 개선했는지 **held-out 검증셋**으로 정량 측정한 기록이다. 재현 스크립트: [`training/eval_stt.py`](training/eval_stt.py).

## 평가 대상
- **모델**: `openai/whisper-small`(244M)에 4지역(경상·전라·충청·강원) 방언 음성으로 **LoRA 파인튜닝**(r=32, q/v proj) → 병합. 어댑터 14MB.
- **기준선(baseline)**: 파인튜닝하지 않은 표준 `whisper-small`.

## 평가 방법
- **검증셋**: 지역별 held-out `val`(학습에 미사용). 분할은 **세션(화자·녹음) 단위 해시**라 같은 화자가 train/val에 걸치지 않음(화자 누수 방지).
- **표본**: 지역별 **전체 val**(경상 1,811 / 전라 451 / 충청 936 / 강원 1,415, 계 4,613 발화). 일부 표본이 아닌 전량 평가.
- **지표**: **CER**(문자 오류율, `jiwer`) 주지표 + **정확일치율**(전사 완전일치 비율).
- **디코딩**: `generate(max_new_tokens=128, no_repeat_ngram_size=3)`, 언어=한국어 고정. 두 모델 동일 조건.

## 결과

| 지역 | 발화 수 | 표준 CER | **파인튜닝 CER** | 상대 개선 | 정확일치 |
|---|---:|---:|---:|---:|---:|
| 경상 | 1,811 | 16.50% | **8.79%** | 47% ↓ | 25% |
| 전라 | 451 | 14.56% | **7.38%** | 49% ↓ | 28% |
| 충청 | 936 | 19.34% | **11.53%** | 40% ↓ | 10% |
| 강원 | 1,415 | 27.59% | **16.91%** | 39% ↓ | 25% |
| **가중평균** | **4,613** | **~20.3%** | **11.70%** | **~42% ↓** | — |

4지역 모두 표준 대비 유의하게 개선됐고, 전 지역 평균 **CER 20.3% → 11.70%**(상대 42% 감소).

## 오류 유형 (정성 분석)
정확일치(전 지역 10~28%)를 제외한 오류는 **대부분 의미가 보존되는 경미한 차이**이며 유형은 다음과 같다:
- **띄어쓰기** — 복합어를 붙이거나 떼는 차이
- **조사·어미** — 방언 특유 종결어미, 조사 유무 차이
- **동음/유사음 한 음절** 치환

문장의 의미는 대체로 보존되며, 후단 변환(KoBART)과 결합 시 실사용 영향은 CER 수치보다 작다.

> 데이터 이용정책(AI 허브)상 원문 전사 예시는 게재하지 않는다.

## 분석
- **가장 강한 지역**: 전라(7.38%), 경상(8.79%). **가장 약한 지역**: 강원(16.91%) — 표준 모델도 강원에서 27.59%로 특히 약하며, 방언 특이성이 커 잔여 오류가 크다.
- **표준어 능력 보존**: full 파인튜닝 대신 **LoRA**(원본 가중치 동결)로 학습해, 표준 Whisper의 일반 한국어 인식 능력을 유지하면서 방언만 보강. 어댑터가 14MB로 작아 온디바이스·지역확장에 유리.
- **학습 방식**: 4지역 균형 데이터를 청크로 나눠 어댑터를 이어학습. 검증 성능 기준 **최적 체크포인트를 선택**(후반 청크에서 소폭 과적합 드리프트 관찰).
- **디코딩**: greedy→**beam search(5)** 로 재학습 없이 CER 추가 **−0.72%p**(9.32→8.60%, n=400/지역). 서빙에 적용. 추론 지연이 ~5배라 온디바이스에선 beam 축소(3 등) 절충 고려. 재현: [`training/eval_beams.py`](training/eval_beams.py).
- **지역 보강(충청)**: 최약 지역(충청) 보강 위해 충청 추가 음성(2만 클립) + 4지역 균형 rehearsal로 증강 이어학습. 결과 **4지역 전부 개선/유지**(가중 CER 9.32→8.93%, n=400 greedy; 경상 −1.0, 충청 −0.5%p) — **forgetting 없음**. 균형 rehearsal이 타 지역 저하를 막음.

## 실환경 강건성 (소음 증강 테스트)

깨끗한 val은 낙관적 상한이므로, held-out 클립에 **소음·전화 협대역 코덱을 합성**해 CER 변화를 측정했다(녹음 불필요, 지역당 200발화). 재현: [`training/eval_robust.py`](training/eval_robust.py).

| 지역 | clean | SNR15(중간소음) | SNR5(강소음) | phone8k(협대역) | phone+SNR10(실제 폰) |
|---|---:|---:|---:|---:|---:|
| 경상 | 8.90% | 16.91% | 35.18% | 9.97% | 27.96% |
| 전라 | 7.53% | 13.34% | 28.85% | 8.58% | 21.31% |
| 충청 | 12.36% | 22.18% | 39.02% | 18.11% | 38.00% |
| 강원 | 9.95% | 11.78% | 21.76% | 12.93% | 19.56% |
| **평균** | **9.69%** | **16.05%** | **31.20%** | **12.40%** | **26.71%** |

- **전화 협대역만으로는 거의 열화되지 않음**(+2.7%p) — Whisper가 대역폭 손실에 강함.
- **주된 열화 요인은 소음**: 중간소음 ~1.7배, 강소음 ~3.2배. **실제 폰+생활소음 조건은 ~27%**로 조용한 환경(~10%)의 약 2.7배.
- 결론: **조용한 환경에선 실사용 가능(~10%), 소음 환경에선 급격히 저하**. 실환경 개선의 1순위는 소음 강건성(학습 시 소음 증강)이다.

### 소음 증강 학습 (최종 채택)
학습 배치에 **on-the-fly 소음/코덱 증강**(매 배치 랜덤 SNR 소음 + 확률적 전화 협대역, `--augment`)을 적용해 4지역 전체를 재학습(평가는 clean). 콜레이터에서 배치마다 증강해 map 캐시가 생기지 않아 디스크·시간 부담 없음.

| 조건 | 원본(무증강) | **증강(최종)** | 변화 |
|---|---:|---:|---:|
| clean | 9.69% | 9.72% | ~동일 |
| SNR15 (중간소음) | 16.05% | 13.60% | **−2.5%p** |
| SNR5 (강소음) | 31.20% | 25.18% | **−6.0%p** |
| phone8k (협대역) | 12.40% | 11.24% | −1.2%p |
| phone+SNR10 (실제 폰) | 26.71% | **20.84%** | **−5.9%p** |

**clean 정확도를 유지하면서 소음 조건 CER을 전반적으로 크게 개선**(실제 폰 조건 −5.9%p, ~22% 상대 감소). 이 증강 모델을 최종 서빙 모델로 채택. 재현: [`training/eval_robust.py`](training/eval_robust.py).

## 파이프라인 End-to-End 평가 (음성 → STT → 변환 → 표준어)

사용자 체감 최종 지표. STT val 클립에 라벨의 표준어(`standard_form`)를 정답으로 부착(클립 `stem+발화index`, 4지역 100% 매칭)하고, **음성부터 최종 표준어 텍스트까지의 CER**을 측정. 재현: [`training/build_e2e_val.py`](training/build_e2e_val.py) + [`training/eval_e2e.py`](training/eval_e2e.py) (지역별 400).

현재 서빙 모델(충청 보강 + beam search) 기준:

| 지역 | 변환기만(완벽STT 가정) | **E2E(실제 음성→표준)** |
|---|---:|---:|
| 경상 | 0.49% | 8.30% |
| 전라 | 0.40% | 6.16% |
| 충청 | 0.61% | 10.55% |
| 강원 | 0.50% | 8.87% |
| **가중평균** | **0.50%** | **8.47%** |

- **최종 E2E CER 8.47%**(문자 정확 ~91.5%) — 조용한 환경에서 "사투리 음성 → 표준어 텍스트"의 사용자 체감 정확도.
- 개선 이력: 초기 4지역 모델(greedy) **9.49% → 현재(충청 보강+beam) 8.47%**(−1.02%p). 4지역 전부 개선.
- **변환기(KoBART)는 완벽STT 가정 시 0.50%로 거의 무손실** → E2E는 STT 오류가 지배, 개선도 전부 STT 향상에서 옴.
- **실환경 E2E**(소음 합성, n=120): 조용 **9.17%** → 실제 폰+생활소음 **19.93%**(경상 17.1/전라 15.0/충청 29.1/강원 18.5%). 소음 증강 등으로 실환경 강건성 개선(참고: 증강 전 STT 소음 조건 ~27%). 충청 소음이 최약점. 재현: [`training/eval_e2e_robust.py`](training/eval_e2e_robust.py).

## 온디바이스 양자화 실현가능성

on-device 배포 시 양자화가 정확도를 해치는지 검증(torch 동적 int8, whisper.cpp GGUF의 보수적 프록시). 재현: [`training/quant_eval.py`](training/quant_eval.py), [`training/quant_eval_kobart.py`](training/quant_eval_kobart.py).

| 모델 | fp32 CER | int8 CER | 손실 | 크기(fp32→int8) |
|---|---:|---:|---:|---:|
| STT (Whisper, n=80/지역) | 10.17% | 10.42% | +0.25%p | 967 → ~242MB |
| 변환 (KoBART, n=150/지역) | 0.92% | 0.93% | +0.00%p | 495 → ~124MB |
| **합계** | | | **무시 가능** | **~366MB** |

- **두 모델 모두 int8 양자화 후 정확도가 사실상 유지**(STT +0.25%p, KoBART +0.00%p). 실물 배포용 whisper.cpp q5는 보통 이보다 우수(~190MB).
- 온디바이스 모델 총 **~366MB**, 첫 실행 다운로드 방식이면 설치 ~50MB.
- 결론: **양자화 후에도 정확도가 유지돼 온디바이스 배포가 현실적**. (네이티브 통합은 별도 작업 — whisper.cpp/onnxruntime + 커스텀 EAS 빌드.)

## 한계 / 유의점
- 본 검증셋은 AI Hub 방언 데이터의 비교적 **정제된 대화·낭독 음성**이다. 실제 앱 환경(휴대폰 마이크, 배경소음, 자유발화)에서는 CER이 더 높아질 수 있어, 위 수치는 **낙관적 상한**에 가깝다.
- 지역별 val 크기가 달라(전라 451 ~ 경상 1,811) 지역 간 직접 비교 시 표본 편차를 감안해야 한다.
- 지역당 학습 클립은 약 2만(포트폴리오 규모)으로, 데이터를 늘리면 특히 강원·충청에서 추가 개선 여지가 있다.
