# 사투리 → 표준어 음성 번역 앱 — 프로젝트 컨텍스트

취업 포트폴리오용 두 번째 프로젝트 (첫 번째는 RouteMate — 산문 할일에서 장소를
추출해 경로/소요시간을 계산해주는 Electron 앱, 완성됨). 이번 프로젝트는 플레이
스토어에 실제로 출시하는 것이 목표이며, "모델을 직접 학습시킨 경험"과 "앱을
배포한 경험"을 동시에 채우기 위해 기획됨.

## 프로젝트 개요

사투리(경상도) 음성을 녹음하면, 표준어 텍스트로 변환해주는 안드로이드 앱.

- 부모님/조부모님 세대의 사투리를 잘 못 알아듣는 자녀 세대, 혹은 사투리 콘텐츠의
  자막화/고객센터 등에 실사용 가치가 있는 시나리오로 스토리텔링.
- "표준 모델이 잘 못하는 영역(사투리)의 원인을 분석하고 데이터로 개선한다"는
  방법론을 핵심 스토리로 삼는다.

## 파이프라인

```
[사투리 음성 녹음] → [STT: 사투리 음성 → 사투리 텍스트] →
[텍스트 변환: 사투리 텍스트 → 표준어 텍스트] → [표준어 텍스트 (+선택적 TTS)]
```

## 기술 스택 / 아키텍처 결정사항

- **아키텍처**: 서버 기반. 앱은 음성을 서버(FastAPI)로 전송하고 결과만 받아온다.
  온디바이스 추론(ONNX/TFLite 변환, 양자화 등)은 지금 범위에서 제외 — 나중에
  여유가 되면 심화 확장 포인트로 남겨둔다.
- **지역**: 경상도 사투리부터 시작. (데이터량이 가장 풍부하고, 억양이 뚜렷해
  데모 임팩트가 좋아서 선택됨. 제주도 등 어휘 자체가 크게 다른 지역은 난이도가
  훨씬 높아 이후 확장 대상으로만 고려.)
- **STT (1단계)**: OpenAI Whisper의 작은 버전(`whisper-base` 또는 `small`)을
  경상도 사투리 음성 데이터로 파인튜닝. 표준 Whisper는 사투리 발음/억양에서
  인식률이 떨어지므로, 이 격차를 줄이는 것이 첫 번째 "모델 개선" 스토리가 됨.
  - **추론 엔진**: `faster-whisper`(CTranslate2). 내부에 오디오 디코더가 번들되어
    시스템 ffmpeg 없이 동작하고 CPU에서 기본 Whisper보다 빠름. 파인튜닝한 모델은
    CTranslate2로 변환해 `WHISPER_MODEL_DIR`로 연결한다.
- **텍스트 변환 (2단계)**: KoBART 또는 소형 T5 계열을 seq2seq로 파인튜닝해
  사투리 텍스트 → 표준어 텍스트로 변환.
- **데이터**: AI Hub(aihub.or.kr)의 "한국어 방언 발화(경상도 등) 데이터"를
  활용 — 음성 파일 + 사투리 전사 텍스트 + 표준어 대응 텍스트가 세트로 제공되어,
  STT와 텍스트 변환 모델 둘 다 이 데이터 하나로 학습 가능.
- **모델 학습 환경**: Google Colab / Kaggle Notebook의 무료 GPU.
- **백엔드**: FastAPI (RouteMate와 동일 패턴 재사용).
- **앱**: 안드로이드, 플레이스토어 출시 목표. **프론트엔드는 React Native(Expo)로
  확정.** 이유: RouteMate(Electron)에서 쌓은 React/JS 경험 재사용, 앱 로직이
  "녹음→서버 전송→결과 표시"로 단순해 네이티브 성능이 불필요, Expo의 `expo-av`
  녹음과 EAS Build로 플레이스토어 빌드/서명까지 빠르게 도달.

## 참고: 관련 실무 경험 (면접 연결 포인트)

- Stable Diffusion 텍스트→이미지 생성 경험 있음 (이 프로젝트와 직접 관련은
  없으나, 생성형 AI 도구 활용 경험으로 참고 가능).

## 진행 현황

- [x] 프론트엔드 프레임워크 확정: React Native(Expo)
- [x] STT/텍스트변환을 하나로 잇는 API 설계 및 **E2E 뼈대 구현·검증 완료**
      (`backend/` — FastAPI `/transcribe`, faster-whisper `base` STT, 규칙기반
      스텁 변환기). `/health`·`/transcribe`·확장자 검증까지 로컬에서 동작 확인.
- [x] **앱 뼈대 구현·번들 검증 완료** (`mobile/` — Expo SDK 57, `expo-audio`
      녹음 → `/transcribe` 호출 → 사투리/표준어 결과 화면). 서버 주소는 앱에서 수정 가능.
      업로드는 `expo-file-system`의 `File.upload`(multipart) 사용 — SDK 57에선
      전역 fetch의 `{uri,name,type}` FormData 파트가 막혀 있어 이 방식이 필요.
      로컬 에뮬레이터에서 녹음→업로드→변환 왕복 실측 확인함.
- [x] **앱 UI 리디자인**(유쾌한 톤: 크림 배경·컬러 블롭·이모지·말풍선 결과·녹음
      펄스 애니메이션) + 카피에서 "경상도" 제거해 **전국 사투리 대상**으로 일반화.
- [~] **범위 확장**: 경상도만이 아니라 **AI Hub가 제공하는 전 지역 방언**을 커버
      목표(데이터 확보/난이도에 따라 지역별 단계적 확장). 앱 문구·브랜딩은 이미
      전국 기준으로 맞춤.

## 아직 정해지지 않은 것 / TODO

- AI Hub 데이터셋 정확한 다운로드/전처리 절차 확인 필요 (계정·승인 필요, 수동)
- Whisper 파인튜닝(Colab) → CTranslate2 변환 → `WHISPER_MODEL_DIR` 연결
- 규칙기반 스텁 변환기 → KoBART/T5 seq2seq 추론으로 교체
- 앱 실기기/에뮬레이터 실측(마이크 녹음→서버 연동) — 코드는 완료, 실행 테스트 남음
- 플레이스토어 출시를 위한 개발자 계정 등록, 개인정보처리방침 등 배포 요건 준비
