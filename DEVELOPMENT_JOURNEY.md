# 사투리 번역기 개발 기록

코드와 함께 관리하는 개발 기록이다. 기능을 만들 때마다 무엇을 고민했고 왜 그렇게 정했는지, 어떤 문제를 만나 어떻게 풀었는지를 남긴다. 처음 설계는 Part 0에, 이후 바꿔 나간 과정은 Part B에 정리한다.

- 프로젝트: 사투리 → 표준어 음성 번역 안드로이드 앱. 사투리 음성을 녹음하면 표준어 텍스트로 바꿔 준다.
- 스택: React Native(Expo) 프론트, FastAPI + Python 백엔드, Whisper(STT)·KoBART(사투리→표준어 변환), AI Hub 한국어 방언 데이터, 로컬 GPU(RTX 3080) 학습.
- 저장소: https://github.com/ysb2152/translate (public, 임시명 `translate`. backend/mobile 모노 구조).

---

## 0. 업데이트 이력 (최신순)

| 시점 | 구분 | 한 일 | 관련 |
|---|---|---|---|
| 2026-09-04 | result | **변환기 온디바이스 동작 성공** — 에뮬에서 "밥 문나 아직 안 무따"→"밥 문나 아직 안 먹었다"(사투리→표준어), **8.5초**. 빈 출력 버그 = decoder 출력 dataPtr이 ArrayBuffer라 argmax 실패 → Float32Array 래핑으로 수정(+int32 재export). Python .pte 재현으로 모델 정확성 먼저 확인 후 JS 진단으로 특정 | B-15 |
| 2026-09-04 | result | **변환기 온디바이스 JS 통합 — 기기에서 로드·실행 확인** — TokenizerModule+ExecutorchModule×2(enc/dec .pte)+greedy 루프가 에뮬에서 로드·실행됨. 남은 문제: 출력 빈 문자열(생성 로직 디버깅), 문장당 ~273s(portable+KV캐시 없음+에뮬 → int8/XNNPACK/KV캐시/실기기 최적화 필요). 리소스페처 exports 패치 함정 기록 | B-15 |
| 2026-09-04 | result | **KoBART → ExecuTorch .pte 변환 성공** — encoder(265.5MB)+decoder(322.5MB, 단일스텝) torch.export→to_executorch. 별도 venv(C:/et), portable(flatc 없이). 변환기 온디바이스 = 런타임 빌드+모델변환 양축 증명. 다음: JS useExecutorchModule 생성루프+토크나이저, int8/XNNPACK 최적화 | B-15 |
| 2026-09-04 | result | **executorch 빌드 통과 — 변환기 런타임 확보** — react-native-executorch 0.9.3 설치+빌드 BUILD SUCCESSFUL(RN0.86), APK에 libexecutorch.so(arm64+x86_64)+whisper.rn 공존(264MB). onnxruntime 대신 executorch로 KoBART 온디바이스 경로 열림. 다음: KoBART→.pte export + JS 생성루프 | B-15 |
| 2026-09-03 | result | **변환기 RN 런타임 비호환 발견** — onnxruntime-react-native 1.24.3이 SDK57/RN0.86/gradle9에서 빌드 실패(`SoftwareComponent 'release' not found`, project ':expo'). 되돌려 whisper.rn green 유지. 모델 export/양자화는 정상, RN 통합만 막힘 → 하이브리드(STT 온디바이스+변환기 서버) 권장, 계획서 §3 | B-15 |
| 2026-09-03 | result | **온디바이스 STT 다중샘플 측정** — 기기에서 4지역 val 8개(지역당 2개) 인식. **평균 CER 5.7%**(서버 val 수준), 지연 ~31s/문장(에뮬 x86_64+f16). 정확도 손실 없이 온디바이스 동작 확인, `EVALUATION.md` | B-15 |
| 2026-09-03 | result | **온디바이스 STT 실기 검증 성공** — 에뮬레이터에서 우리 f16 모델 로드+인식. 결과 "이날치라고 그 국악이 가미된 밴드"(정답 대비 조사 1글자 차) — 서버와 동일 수준. 34s(에뮬 x86_64 CPU+f16, 실기기+q5면 빨라짐). buffer 폴리필·앱 내부경로·adb reverse 등 함정 해결 | B-15 |
| 2026-09-03 | result | **whisper.rn 네이티브 빌드 통과** — whisper.rn 0.7.4 설치 + prebuild + `assembleDebug` **BUILD SUCCESSFUL**(467 태스크). whisper.cpp C/C++가 RN0.86/New Arch에서 컴파일, APK에 librnwhisper.so(arm64/armeabi+CPU변형) 포함. STT 네이티브 모듈 호환 리스크 해소, 다음은 JS 연결+온디바이스 인식 | B-15 |
| 2026-09-03 | result | **버전 호환 스파이크 통과** — 폴더 영문 이전(`뭐라는겨`→`saturi-translator`) 후 prebuild 재생성 + `gradlew assembleDebug` **BUILD SUCCESSFUL**(427 태스크, app-debug.apk 171.8MB). RN0.86/React19/New Arch 네이티브 빌드 정상 확인. 리포·아티팩트·venv 온전 | B-15 |
| 2026-09-03 | result | **버전 호환 스파이크 — 한글 경로가 빌드 블로커** — expo-dev-client + prebuild(newArchEnabled=true 확인) 후 gradle 빌드가 한글 경로 인코딩으로 실패(plugin 해결 불가). ASCII 경로 복사 시 `BUILD SUCCESSFUL`. SDK57/RN0.86/New Arch 자체는 정상 → 로컬 폴더 영문 이전으로 해결 | B-15 |
| 2026-09-02 | result | **STT(Whisper) GGUF 변환 검증** — 앱 없이 dev PC에서 파인튜닝 병합모델을 whisper.cpp `convert-h5-to-ggml.py`로 변환. `ggml-model-f16.bin` 487MB, ggml 매직 유효. 우리 모델이 whisper.cpp 포맷으로 깨끗이 변환됨 확인. q5 양자화는 C 빌드(컴파일러) 필요로 보류 | B-15 |
| 2026-09-02 | result | **변환기(KoBART) ONNX int8 검증** — 앱 없이 dev PC에서 최대 리스크 선(先)검증. optimum ONNX export(encoder/decoder/with-past) + onnxruntime int8. 변환기 **268MB**, PyTorch 대비 생성 **정합성 5/5 완전 일치**. 리스크가 "모델 export"→"앱 JS 토크나이저/생성루프"로 좁혀짐. `backend/onnx_quant_parity.py` | B-15 |
| 2026-09-02 | feat | **배포 준비(공통 선행)** — eas.json 빌드 프로파일, app.json 릴리스 식별자(com.ysb2152.saturitranslator·versionCode), 앱 내 AI허브 출처 표기, 개인정보처리방침 초안(백엔드 즉시삭제 동작 확인 후 문구 일치). 온디바이스 vs 서버는 다음 단계 결정 | B-15 |
| 2026-09-02 | decision | **충청 재보강 기각** — 재학습 전 충청 val 오류 유형 분석(beam=5, n=300). 치환 97% 1회성(산발적)·반복 오류는 표기 규약(것/거, 띄어쓰기)뿐 → 데이터 증량 표적 없음, 남은 실질오류는 whisper-small 한계. 배포로 전환 | B-11 |
| 2026-09-02 | result | **증강 강화 학습(잔향+SpecAugment+harder SNR)** — 소음을 더 밀기 위해 증강 강화 재학습. 소음 E2E 19.62→**19.02%(-0.6%p, 충청 29→27%)**, clean은 8.29→8.67% 소폭↑(트레이드오프). 실환경 우선 채택. whisper-small **소음 ~19% 천장** 확인 → 정확도 수확체감, 다음은 배포로 전환 | B-11 |
| 2026-09-01 | result | **데이터 확대 + 실제소음(MUSAN) 증강 학습** — 확대 균형셋(54k) + MUSAN 930개 실제소음 mixing 1에폭. clean 전 지역 추가 개선(STT 가중 8.93→8.12%, E2E clean 9.17→8.29%), 소음 조건은 거의 불변(19.9→19.6%, 실제소음 1에폭 한계). 채택 | B-10 |
| 2026-09-01 | result | **충청 보강 학습** — 충청 추가 음성(충청도_7) 2만 클립 + 4지역 균형 rehearsal로 증강 이어학습. 4지역 **전부 개선/유지**(가중 CER 9.32→8.93%, n400 greedy), **forgetting 없음**. 채택. 온디바이스 int8 양자화 정확도 유지(+0.25%p) 검증도 완료 | B-10 |
| 2026-09-01 | result | **STT 소음 증강 4지역 전체 재학습 완료** — on-the-fly 소음/협대역 증강(`--augment`)으로 4청크 이어학습. 원본 대비 실제 폰 조건 CER **26.7%→20.8%(-5.9%p, ~22%↓)**, 강소음 -6.0%p, clean 유지(9.7%). 최종 서빙 모델 채택 | B-10 |
| 2026-08-31 | result | **STT 소음 증강 학습 채택(1청크 검증)** — 학습 배치에 on-the-fly 소음/전화 협대역 증강(`--augment`, 평가는 clean, map 캐시 없음). 1청크 증강만으로 실제 폰 조건 CER **26.7%→21.5%(-5.2%p)**, clean 유지. `EVALUATION.md` | B-10 |
| 2026-08-31 | result | **STT 4지역 LoRA 완성 + 정량평가** — 경상·전라·충청·강원 균형 학습(청크 이어학습, 최적 체크포인트 선택). held-out **전체 val** 지역별 평가: 표준 대비 CER **20.3%→11.7%**(가중평균, 경상8.8/전라7.4/충청11.5/강원16.9%). 소음 강건성 테스트: 조용~10%, 실제 폰+소음 ~27%. `EVALUATION.md`, `training/eval_stt.py`·`eval_robust.py` | B-10 |
| 2026-08-31 | feat | STT 나눠 학습(`--resume-adapter`) — 4지역 균형 데이터를 청크로 나눠 어댑터 이어학습(디스크 캐시 축소로 대용량 학습 크래시 회피, 세션당 ~25분) | B-10 |
| 2026-08-31 | result | **STT LoRA 방식 검증**(경상+전라 2지역) — 표준 대비 CER 14.6%→**7.38%**(50%↓). full-FT(경상만 6.69%)와 경상 val 기준 0.7%p 차이인데, 어댑터 **1.44%(353만)**만 학습하고 2지역으로 희석됐음에도 근접 → **원본 표준어 능력 보존 + 지역 커버 확대 + 작은 어댑터** 이득이 커 LoRA 채택. 4지역까지 LoRA로 진행 | B-10 |
| 2026-08-31 | feat | STT 충청도 Validation 음성 전처리로 **2만 클립**(train 19.4K/val 936), 매칭 100%(38/38). 경상·전라·충청 3지역 준비(~6만 클립). 강원은 라벨만 확보, 음성 재다운로드 예정 | B-10 |
| 2026-08-31 | feat | STT 4지역 확대 착수 — 전라도 Validation 음성(전라도_3.zip, tar 분할zip)에서 스트리밍 전처리로 **2만 클립**(train 19.7K/val 451) 생성, 라벨 매칭 100%(86/86 세션). `data/processed_jl/stt`. 원본 zip은 처리 후 삭제 | B-10 |
| 2026-08-31 | feat | 앱 에뮬레이터 실기 테스트 — small STT 파이프라인 동작 확인. 데모(샘플) 버튼은 `__DEV__` 게이팅해 **릴리스 빌드에선 숨김**. 관찰: STT가 발음 유사어로 오인식하는 사례 있음(경상 편중 학습·small 한계 → 후속 데이터 보강/디코딩 튜닝 과제) | B-10 |
| 2026-08-30 | result | **STT를 whisper-small로 재학습** — 표준 대비 CER **14.6%→6.7%(54% 개선)**, base(10.2%)보다 우위. effective 배치 16 고정(4×누적4)으로 base와 동일 조건→모델 효과 분리. GPU 메모리(10GB+디스플레이)·타 앱(크롬/디스코드) VRAM 경쟁으로 batch 튜닝 필요했음 | B-10 |
| 2026-08-30 | feat | 파인튜닝 Whisper·KoBART **백엔드 서빙 연결** — 앱 파이프라인(STT→변환) 통합 확인. ct2 변환기-transformers 버전 충돌로, Whisper는 CTranslate2 대신 transformers로 직접 서빙(오디오 디코딩은 faster-whisper 유틸 재사용) | B-10 |
| 2026-08-30 | result | **STT(Whisper) 파인튜닝 완료** — 표준 Whisper 대비 사투리 CER **18.6%→10.2%**(45% 개선). 로컬 RTX 3080에서 whisper-base 2에폭 ~17분. baseline/파인튜닝 비교 측정 | B-10 |
| 2026-08-30 | feat | 로컬 GPU(3080) 학습 환경 구축 — CUDA torch, KoBART/Whisper 학습 스크립트. Colab GPU 한도·지오블록에서 벗어남 | B-9,B-10 |
| 2026-08-29 | feat | STT 학습데이터 준비 완료 — 경상 Validation 음성(라벨과 100% 매칭)에서 **2만 클립**(train 18.2K/val 1.8K) 스트리밍 생성. AI Hub가 tar 안에 zip을 1GB씩 쪼개 줘서(`.zip.partN`), 재구성 없이 tar에서 이어붙여 읽는 가상 스트림으로 처리 | B-10 |
| 2026-08-29 | result | **다지역 변환 모델 완료**: 4지역(경상/전라/충청/강원) 70만 학습셋으로 재학습 → copy 대비 CER **5.79%→1.63%**, 정확일치 **26.5%→77.7%**. 4지역 모두 번역 확인, 백엔드 서빙 반영 | B-9 |
| 2026-08-29 | feat | **다지역 확장**: 경상+전라+충청+강원 4지역 통합 변환 학습셋(train **193만**, 변형 135만). 전라도는 라벨이 .json 아닌 **.txt(이중전사)** 로 와 `parse_txt_label` 추가로 처리. AI Hub 다운로드가 `.tar`(내부 `.zip.part0`)로 와서 판별·추출 과정 필요 | B-9 |
| 2026-08-29 | feat | 학습한 KoBART를 **백엔드 서빙에 연결** — torch(cpu)+transformers 설치, `models/kobart-dialect` 자동 감지, 실제 추론 확인(반복 없이 정상 번역) | B-9 |
| 2026-08-29 | result | **KoBART 변환 모델 성과**: copy 기준선 대비 CER **4.62%→1.01%**, 정확일치 **29.6%→82.2%**. "데이터로 개선"을 정량 확인 | B-9 |
| 2026-08-29 | fix | KoBART 무한반복 디버깅 — 번역은 맞는데 정답 뒤 반복(CER 폭발). 원인은 KoBART 토크나이저가 **EOS를 자동 미부착**해 라벨에 종료신호가 없던 것. 라벨 끝에 EOS 추가 후 재학습으로 해결 | B-9 |
| 2026-08-29 | feat | STT 스트리밍 전처리기(`preprocess_streaming.py`) — 28GB 음성 zip을 안 풀고 WAV 하나씩 꺼내 클립화→즉시 삭제(외장하드 없이 34GB 내 처리). 합성 zip E2E 테스트 통과 | B-10 |
| 2026-08-29 | docs | GitHub README 작성 — 파이프라인·EDA 지표·기술스택·실행법·구조 | - |
| 2026-08-29 | feat | 데이터 EDA(`data/analyze.py` → `data/analysis.md`) — 208만 쌍 중 변형 16.1%, 최다 치환 `쫌→조금` 13.9만회 등 | B-9 |
| 2026-08-29 | docs | 파인튜닝 근거 문서(`why_finetune.md`) 작성 — 모델 선택·방식·이유 + 하이퍼파라미터 값 근거 + 심화 주제 | B-9 |
| 2026-08-29 | feat | 변환 모델 트랙: 균형 학습셋 구성(`build_mt_dataset.py`, 45.5만 쌍) + KoBART 파인튜닝 노트북 + 백엔드 `DialectConverter` KoBART 연동(규칙 폴백). 음성 없이 진행 | B-9 |
| 2026-08-29 | feat | 실제 경상도 라벨(학습데이터_1, 7,699세션·209만 발화)로 전처리 → **변환 문장쌍 209만쌍**(실 방언변형 약 33만) 생성. 파서가 실제 스키마와 일치 확인. 대용량에서 오디오 탐색 O(n²) 병목을 인덱스로 수정 | B-7 |
| 2026-08-28 | fix | AI Hub **해외 IP 다운로드 차단(지오블록)** 확인 → Colab 직접 다운로드 불가. "한국 IP(집 PC)에서 받아 Drive로 옮겨 학습"으로 계획 수정, 노트북·README에 경고 | B-8 |
| 2026-08-28 | feat | 용량 문제 대응: Colab 노트북(`notebooks/aihub_colab.ipynb`) 작성 — aihubshell로 **부분 다운로드** → 전처리 → Drive 저장 | B-8 |
| 2026-08-28 | feat | AI Hub 방언 데이터 전처리 파이프라인 작성(`data/`) — 라벨 JSON → STT 매니페스트 + (방언→표준) 문장쌍, 오디오 구간 슬라이스, 합성 샘플로 E2E 테스트 통과 | B-7 |
| 2026-08-28 | chore | GitHub 공개 저장소 `translate` 생성·푸시(코드와 함께 개발기록 관리). Expo 내부 .git이 서브모듈로 잡히던 문제 정리 | B-6 |
| 2026-08-28 | design | 스타일 5방향 탐색 후 **"한지 × 클린 하이브리드"** 채택, `App.js` 재구현(한지빛 배경·먹색 세리프·청록 포인트·View 마이크·클린 결과카드) | B-5 |
| 2026-08-28 | chore | 범위 확장 반영: 앱 카피에서 "경상도" 제거 → **전국 사투리** 대상으로 일반화 | B-5 |
| 2026-08-28 | fix | 업로드 실패(`unsupported FormDataPart`) 원인 규명 후 `expo-file-system`의 `File.upload`(multipart)로 해결, 왕복 실측 확인 | B-4 |
| 2026-08-28 | chore | 안드로이드 에뮬레이터를 커맨드라인 도구로 구축(JDK17·SDK·AVD `saturi_pixel6`), WHPX 가속 확인 | B-3 |
| 2026-08-28 | feat | 앱 뼈대(Expo SDK 57, `expo-audio` 녹음 → `/transcribe` 호출 → 결과 화면) | B-2 |
| 2026-08-28 | feat | 백엔드 E2E 뼈대(FastAPI `/transcribe`·`/health`, faster-whisper STT, 규칙 스텁 변환기) | B-1 |
| 2026-08-28 | init | 기획 정리 및 기술 결정(React Native/Expo, faster-whisper 확정) | Part 0 |

초기에는 학습보다 **파이프라인이 앱에서 끝까지 흐르는 뼈대를 먼저 관통시키고 디자인 방향을 확정**했다. 이후 변환(KoBART)과 STT(Whisper)를 순서대로 학습해 규칙 스텁·기본 Whisper 자리를 교체했고, "표준 모델이 못하는 사투리를 데이터로 개선한다"는 핵심을 두 단계 모두에서 정량으로 증명했다.

---

# Part 0. 프로젝트의 출발점

처음 이 프로젝트를 구상했을 때의 설계와 계획이다. 이후의 변화는 모두 여기에서 출발한다.

**처음의 목표와 형태.** 플레이스토어에 실제 출시하는 안드로이드 앱으로, 사투리 음성을 표준어로 옮기는 것을 목표로 잡았다. 부모·조부모 세대의 사투리를 잘 못 알아듣는 자녀 세대, 사투리 콘텐츠의 자막화·고객센터 같은 실사용 시나리오를 상정했다.

**처음의 기술 선택.** 아키텍처는 서버 기반으로 정했다. 앱은 음성을 FastAPI 서버로 보내고 결과만 받아오며, 온디바이스 추론(ONNX/TFLite 변환·양자화)은 초기 범위에서 제외하고 이후 심화 확장으로 남겼다. 파이프라인은 두 단계로, 1단계 STT가 사투리 음성을 사투리 텍스트로, 2단계 변환이 사투리 텍스트를 표준어 텍스트로 바꾼다. STT는 OpenAI Whisper의 작은 버전을 방언 데이터로 파인튜닝하고, 변환은 KoBART나 소형 T5를 seq2seq로 파인튜닝하는 계획이었다. 데이터는 AI Hub의 한국어 방언 발화 데이터를 쓰기로 했는데, 음성 + 사투리 전사 + 표준어 대응이 세트라 STT와 변환 두 모델을 하나의 데이터로 학습할 수 있기 때문이다.

**처음부터 명확히 하려던 원칙.** 이 프로젝트의 정체성은 알고리즘이 아니라 **데이터로 성능을 개선하는 방법론**이다. "표준 Whisper가 사투리를 못 알아듣는 원인을 분석하고 데이터로 개선한다"가 핵심이다.

**당시의 한계(미정 항목).** 앱 프론트엔드 프레임워크 미확정, AI Hub 데이터의 정확한 다운로드·전처리 절차 미확인, 두 모델을 하나의 파이프라인으로 잇는 API 설계, 플레이스토어 출시 요건(개발자 계정·개인정보처리방침)이 처음의 열린 문제였다. 시작 지역도 처음에는 데이터가 풍부하고 억양이 뚜렷한 경상도로 잡았다(이후 4지역으로 확장).

---

# Part A. 프로젝트 개요와 현재 설계

**풀려는 문제.** 표준 음성인식 모델은 사투리 발음·억양·어휘에서 인식률이 떨어진다. 그 격차를 방언 데이터로 좁혀, 사투리 음성을 표준어 텍스트로 정확히 옮기는 것이 목표다.

**사용자 흐름.** 앱에서 녹음 버튼을 눌러 사투리로 말하면, 녹음 파일이 서버로 업로드되고, 서버가 STT로 사투리 텍스트를 뽑은 뒤 변환 모델로 표준어 텍스트를 만들어 돌려준다. 앱은 "인식된 사투리"와 "표준어" 두 결과를 함께 보여 준다.

**파이프라인.**

| 단계 | 입력 → 출력 | 담당 | 현재 상태 |
|---|---|---|---|
| 1. STT | 사투리 음성 → 사투리 텍스트 | Whisper-small | 4지역 LoRA 파인튜닝 + 소음 증강(완료) |
| 2. 변환 | 사투리 텍스트 → 표준어 텍스트 | KoBART | 4지역 파인튜닝(완료) |
| (선택) TTS | 표준어 텍스트 → 음성 | 미정 | 미착수 |

**확정된 방향.** 프론트엔드는 React Native(Expo)로 정했다(B-2). STT 추론 엔진은 faster-whisper로 시작했고, 파인튜닝 모델은 transformers로 직접 서빙한다(B-1, B-10). 범위는 경상도 한정이 아니라 AI Hub가 제공하는 전 지역 방언을 목표로 넓혔고(현재 4지역), 그에 맞춰 앱 문구도 "전국 사투리" 기준으로 바꿨다(B-5). 디자인은 "한지 × 클린 하이브리드"로, 정체성은 한지 정서(한지빛 배경·먹색 세리프·청록/주칠), 작업 화면은 토스식 명료함(깔끔한 카드·또렷한 위계)으로 잡았다(B-5).

**실패를 고려한 설계.** 앱은 서버 주소를 화면에서 바꿀 수 있게 해 에뮬레이터(`10.0.2.2`)와 실기기(PC의 LAN IP)를 모두 지원한다. 백엔드는 업로드 확장자를 검증하고(415), 파일 크기 상한과 빈 파일을 막으며, STT 실패 시에도 서버가 죽지 않게 예외를 감싼다. 결과가 비어도(무음 등) 화면이 멈추지 않고 안내 문구를 보여 준다.

---

# Part B. 개발 진행 로그

여기부터가 실제로 손을 대며 만들어 간 기록이다. 각 항목은 무엇이 문제였고 어떤 선택을 했으며 왜 그렇게 했는지, 무엇이 어려웠는지를 남긴다.

## B-1. 백엔드 E2E 뼈대와 STT 엔진 선택

학습을 먼저 하지 않고, **파이프라인 전체가 도는 뼈대부터 관통**시키기로 했다. ML 프로젝트가 데이터·학습에만 몇 주 매달리다 앱은 껍데기도 못 만드는 실패를 피하려는 판단이었다. 그래서 STT는 파인튜닝 전 기본 Whisper, 사투리→표준어 변환은 단어 사전 규칙 스텁으로 두고, 녹음 → STT → 변환 → 결과가 끝까지 흐르는지를 먼저 확인했다. 두 모델 자리는 시그니처만 고정해 두어, 이후 학습된 모델로 이 자리만 교체하면 되도록 했다.

STT 추론 엔진은 faster-whisper(CTranslate2)로 정했다. 개발 PC에 시스템 ffmpeg가 없었는데, 표준 openai-whisper는 오디오 디코딩에 ffmpeg가 필요한 반면 faster-whisper는 내부에 디코더(PyAV)를 번들해 시스템 ffmpeg 없이 동작하고, CPU에서도 더 빠르다. (파인튜닝 모델은 이후 CTranslate2 변환에서 버전 충돌이 나 transformers로 직접 서빙하게 된다 — B-10.)

백엔드는 FastAPI로 `POST /transcribe`(오디오 업로드), `GET /health`를 만들고, 모델은 첫 요청 지연을 피하려 기동 시 예열한다. 스텁 변환기(`DialectConverter`)는 "가가→그 아이가", "억수로→굉장히" 같은 최소 사전으로만 동작하며, 2단계에서 KoBART 추론으로 이 클래스만 교체하면 되도록 인터페이스를 유지했다.

사소한 함정으로, faster-whisper가 `requests`를 런타임에 요구하는데 의존성으로 딸려오지 않아 임포트가 실패했다. `requirements.txt`에 명시해 해결했다. 톤 신호 WAV로 배관을 확인했을 때 텍스트는 비었지만(음성이 아니라 VAD가 걸러냄) 모델 로딩·추론·길이 계산이 정상 동작하는 것을 확인했다.

## B-2. 앱 뼈대와 프레임워크 선택

앱 프레임워크는 React Native(Expo)로 확정했다. 후보는 React Native / Flutter / 네이티브(Kotlin)였는데, 이미 React/JS 경험이 있어 학습 곡선이 가장 완만하고, 이 앱의 로직이 "녹음 → 서버 전송 → 결과 표시"로 단순해 네이티브의 성능 이점이 필요 없다고 봤다. Expo의 `expo-audio` 녹음과 EAS Build로 플레이스토어 빌드·서명까지 빠르게 도달할 수 있는 점도 이유였다.

프로젝트는 Expo SDK 57로 생성됐다. SDK 57의 `AGENTS.md`가 버전 고정 문서를 먼저 읽으라고 명시해, 녹음 API는 구식 `expo-av`가 아니라 SDK 57 권장인 `expo-audio`의 훅(`useAudioRecorder`, `useAudioRecorderState`, `RecordingPresets`)으로 구현했다. 마이크 권한 문구는 `app.json`의 `expo-audio` 플러그인 설정에 넣었다. 서버 주소는 앱 화면에서 수정 가능하게 해, 에뮬레이터와 실기기 환경 차이를 코드 수정 없이 흡수하도록 했다.

## B-3. 안드로이드 에뮬레이터를 커맨드라인으로 구축

앱을 실제로 돌려 볼 에뮬레이터가 필요했다. Android Studio 전체를 까는 대신 **커맨드라인 도구만** 설치하는 길을 택했다. 개발용 IDE가 지금은 필요 없고(Expo로 개발), 디스크 여유가 넉넉하지 않아(C: 약 34GB) 가벼운 쪽이 맞았기 때문이다.

설치는 관리자 권한 없이 진행했다. JDK 17을 portable zip으로 풀고(sdkmanager/avdmanager가 Java 앱이라 필요), Android command-line tools를 `cmdline-tools/latest`에 배치한 뒤, `ANDROID_HOME` 등 환경변수를 사용자 범위로 영구 등록했다. sdkmanager로 platform-tools·emulator·`platforms;android-34`·`system-images;android-34;google_apis;x86_64`를 설치하고(라이선스는 stdin 파일 리다이렉션으로 일괄 수락), AVD `saturi_pixel6`(Pixel 6 / API 34)를 만들었다.

가장 걱정한 것은 하드웨어 가속이었다. CPU가 AMD(Ryzen 5 5600X)라 Intel HAXM을 못 쓰고 Windows Hypervisor Platform(WHPX)이 필요한데, `emulator -accel-check`로 확인하니 **WHPX가 이미 설치·사용가능** 상태여서 관리자 권한·재부팅 단계가 통째로 생략됐다. 콜드 부팅이 12~27초로 빨랐다. Expo Go를 에뮬레이터에 설치해 앱을 올리고, 녹음→업로드→변환 왕복까지 확인했다(무음 마이크라 텍스트는 Whisper 환각이 나오지만 배관은 정상). x86_64 이미지를 고른 것도 AMD 호스트에서 네이티브 가상화로 돌리기 위함이었다.

## B-4. 업로드 실패(unsupported FormDataPart) 해결

앱에서 녹음 후 변환하면 `unsupported FormDataPart implementation` 오류가 났다. 처음 구현은 React Native 관례대로 전역 `fetch`에 `FormData`를 만들고 `{ uri, name, type }` 형태의 파일 파트를 append하는 방식이었는데, Expo SDK 57(RN 0.86, New Architecture)에서는 이 구식 파일 파트가 더 이상 지원되지 않는 것이 원인이었다.

해결은 Expo 표준 업로드인 `expo-file-system`의 `File.upload`로 바꾸는 것이었다. SDK 57 문서를 확인해 새 File 클래스 API(`new File(uri).upload(url, { uploadType: UploadType.MULTIPART, fieldName, mimeType, httpMethod })`)가 권장 경로임을 파악하고, 설치된 패키지의 타입 정의에서 반환형(`{ body, status, headers }`)까지 직접 확인한 뒤 `src/api.js`를 재작성했다. `expo-file-system`은 Expo Go에 기본 내장이라 커스텀 빌드 없이 바로 동작한다.

버그를 고친 뒤 에뮬레이터에서 녹음→업로드→변환을 눌러, 결과 카드에 텍스트와 길이(2.25초)가 뜨는 것으로 오류가 사라지고 서버 왕복이 되는 것을 실측했다. 새 패키지를 설치했으므로 실행 중이던 Metro가 이를 알도록 캐시를 비우고(`--clear`) 재시작해야 했던 점(그렇지 않으면 모듈을 못 찾음)도 확인했다.

## B-5. 디자인 방향 탐색과 한지 × 클린 하이브리드

뼈대가 도는 것을 확인한 뒤 앱의 비주얼을 잡았다. 처음에는 "유쾌한" 파스텔 버전을 만들었으나, 방향을 원점에서 다시 잡기로 하고 Canva·미리캔버스·Figma를 참고해 앱에 어울릴 스타일 5방향을 목업으로 비교했다(또랑또랑 클린 / 정겨운 한지 / 보이스 웨이브 / 말랑 파스텔 / 미니멀 에디토리얼). 각 방향을 실제 앱 화면 목업 + 팔레트 + 타이포 + "왜 어울리나"로 나란히 놓아 고르기 쉽게 했다.

선택은 **"한지 × 클린 하이브리드"**였다. 이 앱의 핵심이 "표준 모델이 못하는 사투리를 데이터로 개선한다"는 점이라, 정체성은 한국적 정서(한지빛 배경·먹색 세리프 타이틀·단청에서 뽑은 청록/주칠)로 각인시키고, 녹음·결과 같은 작업 화면은 토스식 명료함(깔끔한 카드·또렷한 위계)으로 신뢰를 주는 조합이 차별화와 완성도를 동시에 잡는다고 판단했다. 청록 하나를 주 강조색으로 통일해 전통색이자 클린한 포인트로 동시에 읽히게 했고, 세리프(정체성)와 산세리프(기능)로 역할을 나눴다.

구현에서는 앱 용량·의존성을 아끼려 정확한 Gowun Batang 대신 안드로이드 기본 세리프(`fontFamily: 'serif'` → Noto Serif CJK)로 한글 세리프 느낌을 냈고, 마이크 아이콘도 이모지 대신 View로 그려 의존성 0으로 유지했다. 녹음 중에는 주칠색과 펄스 링으로 상태를 드러낸다. 이와 함께 프로젝트 범위가 전국 사투리로 넓어진 것을 반영해, 앱 카피에서 "경상도"를 빼고 "전국 사투리"·"사투리로 말하면 표준어로" 기준으로 문구를 일반화했다.

## B-6. GitHub 저장소 구성

코드와 개발기록을 함께 관리하려고 GitHub 공개 저장소 `translate`(임시명)를 만들었다. backend/mobile 모노 구조 그대로 올리고, 이후 이 `DEVELOPMENT_JOURNEY.md`를 커밋과 함께 갱신한다.

첫 커밋에서 함정이 하나 있었다. `create-expo-app`이 `mobile/`을 만들 때 내부에 자체 `.git`을 초기화해 둬서, 루트에서 `git add`를 하면 `mobile`이 파일이 아니라 서브모듈(gitlink, mode 160000)로 잡혔다. 이대로 올리면 앱 소스가 빠진다. `mobile/.git`을 제거하고 인덱스의 gitlink를 강제로 지운 뒤(`git rm --cached -f mobile`) 다시 추가해 앱 소스를 정상 포함시켰다. 무거운 산출물(node_modules, .venv, tmp, dist)은 루트와 하위 `.gitignore`로 제외했다.

## B-7. AI Hub 방언 데이터 전처리 파이프라인

학습에 앞서, AI Hub "한국어 방언 발화" 데이터를 학습용 산출물로 바꾸는 전처리를 만들었다(`data/`). 하나의 데이터에서 두 모델을 학습하는 것이 목표라 산출물도 둘로 나눴다. STT(Whisper) 파인튜닝용 매니페스트(`{audio_filepath, text, duration}`, text는 방언 전사)와, 변환 모델(KoBART)용 문장쌍(`{dialect, standard}`)이다.

가장 큰 제약은 **데이터를 직접 받을 수 없다는 것**이었다. AI Hub는 계정·승인·수동 다운로드가 필요하다. 그래서 실제 데이터 없이도 개발·검증이 가능하도록, 라벨 구조를 흉내 낸 합성 샘플 생성기(`make_sample.py`)와 스모크 테스트(`test_preprocess.py`)를 함께 만들어, 정제·추출·엔드투엔드(샘플 생성 → 전처리 → 산출물·클립 존재 검증)가 한 번에 돌게 했다.

두 번째 제약은 **라벨 스키마가 데이터셋·연도·지역마다 조금씩 다르다는 것**이었다. 웹 조사로는 정확한 필드명을 확정할 수 없어, 특정 필드명에 못박지 않고 방어적으로 설계했다. `aihub_schema.py`의 `FIELDS`에 후보 필드명을 나열해 순차 탐색하고, 문장 필드(`dialect_form`/`standard_form`)가 없으면 어절 리스트(`eojeolList`)로 조립하며, 그것도 없으면 원문(`form`)으로 대체한다. 전사에 섞이는 마커 — `(A)/(B)` 이중전사, `{웃음}`, `(())` 불명확 구간, 어절 뒤 `b/`·`l/` 같은 KsponSpeech 계열 표기 — 는 `clean_text`가 정제하되, 방언은 이중전사의 앞쪽, 표준은 뒤쪽을 고르도록 mode를 나눴다. 실제 파일을 받으면 라벨 하나만 열어 `FIELDS`를 맞추면 대부분 그대로 동작한다.

오디오는 세션 단위 긴 WAV에 발화별 `start`/`end`가 붙는 경우를 기본으로 보고, 표준 라이브러리 `wave`만으로 구간을 잘라 발화 클립을 저장한다(`wav_utils.slice_wav`). 이미 발화 단위로 잘린 데이터면 `--no-slice`로 원본을 참조한다. train/val 분할은 발화가 아니라 **세션(라벨 파일) 단위**로 해서 같은 화자·세션이 두 셋에 걸쳐 새는 것을 막았다. 너무 짧거나(0.3s 미만) Whisper 한계(30s)를 넘는 발화는 제외하고, 방언과 표준이 동일한 쌍의 비율 등은 `stats.json`으로 남겨 데이터 품질을 눈으로 확인할 수 있게 했다.

**실제 데이터 검증(2026-08-29).** 경상도 라벨 zip(학습데이터_1)을 받아 돌려 보니 실제 스키마가 가정과 정확히 일치했다(`utterance[]`에 `dialect_form`/`standard_form`/`eojeolList`/`start`/`end`). 파서 수정 없이 7,699개 세션·208만 발화에서 변환 문장쌍 약 209만 쌍을 뽑았고, 방언과 표준이 다른 실 변형은 약 33만 쌍이었다(예: `묵→먹`, `아이가→아니야`, `가가지고→가서`, `쪼끔→조금`). 나머지 84%는 방언=표준의 동일 쌍인데, 이는 대부분의 어절이 표준과 같은 방언 데이터의 자연스러운 성질이다(학습 때 동일 쌍은 다운샘플링해 변형 학습에 집중시킬 계획). 한 가지 성능 문제도 드러났다. 라벨마다 오디오를 `rglob`으로 찾던 방식이 파일 7,699개에서 O(n²)로 매우 느렸는데, 오디오 basename→경로 인덱스를 한 번만 만들어 dict 조회로 바꿔 해결했다.

## B-8. 데이터 용량 문제와 Colab 부분 다운로드

전처리 파이프라인은 준비됐지만 실제 데이터가 걸림돌이었다. AI Hub 방언 데이터는 지역당 3,000시간이 넘어 수백 GB라, 개발 PC의 여유 공간(약 34GB)에는 전량이 들어가지 않는다. 그런데 이 프로젝트의 목적은 "표준 대비 개선"을 보이는 것이라 전량이 필요하지 않다고 판단했다. 수십 시간, 혹은 그 이하로도 baseline 대비 개선을 측정할 수 있다.

그래서 두 가지로 우회하려 했다. 첫째, AI Hub의 `aihubshell`이 파일 단위(`filekey`) **부분 다운로드**를 지원하므로 원하는 청크만 골라 받는다(경상도 라벨=`572701` 308MB, 음성 한 덩어리=`572702` 28GB 등). 둘째, 학습은 Colab 무료 GPU에서 하니 데이터도 Colab에서 받으려 노트북(`notebooks/aihub_colab.ipynb`)을 만들었다.

그런데 여기서 **결정적 제약**을 만났다. Colab에서 다운로드하니 `AI 허브는 해외에서의 데이터 다운로드를 제한하고 있습니다`로 막혔다. AI Hub는 **해외 IP 다운로드를 차단**한다(Colab 서버는 해외). 처음 뜬 "인증실패/권한거부"도 실은 이 지오블록이었다. 그래서 계획을 **"한국 IP(집 PC)에서 받아서 학습 환경으로 옮긴다"**로 수정했다. 라벨(308MB)·소량은 로컬에서 받아 바로 전처리하고, 음성(zip당 17~28GB)은 스트리밍 전처리기로 안 풀고 클립만 뽑는 방식으로 갔다. 노트북에는 부분 다운로드·증분 처리 흐름을, README에는 지오블록 경고를 남겼다. (이후 학습 환경도 Colab 대신 로컬 GPU로 옮긴다 — B-10.)

## B-9. 사투리→표준어 변환 모델(KoBART) 학습 트랙

STT용 음성(28GB)이 용량·지오블록으로 막혀 있는 동안, **음성이 필요 없는 변환 모델(2단계)부터** 먼저 끝내기로 순서를 바꿨다. 변환 학습 데이터는 라벨만으로 이미 209만 쌍이 나와 있고(B-7), 텍스트라 작아서 지오블록·용량 문제 없이 학습할 수 있기 때문이다. 이렇게 하면 STT(당시 기본 Whisper)와 **학습된 변환기**를 이어 붙여, 28GB 음성 없이도 규칙 스텁보다 개선된 2단계 파이프라인을 데모할 수 있다.

먼저 학습셋을 다듬었다(`data/build_mt_dataset.py`). 209만 쌍에는 완전 중복이 많고 동일쌍(방언=표준)이 84%라, 그대로 학습하면 모델이 '베끼기'만 배우기 쉽다. 그래서 완전 중복을 제거하고, 동일쌍을 목표 비율(기본 30%)까지 다운샘플링해 변형 학습에 집중시켰다. 결과는 train 45.5만 쌍(변형 31.9만 + 동일 13.7만), val 5천 쌍이다.

학습은 `gogamza/kobart-base-v2`를 seq2seq(입력=방언, 타깃=표준)로 파인튜닝했다. 평가는 '그대로 베끼기(copy)' 기준선과 CER·정확일치를 비교해, 모델이 실제로 방언을 표준으로 바꾸는지를 본다(동일쌍이 많아 copy도 어느 정도 맞지만, 변형에서 갈린다). 백엔드도 미리 연결했다. `DialectConverter`가 `CONVERTER_MODEL_DIR`이 있으면 KoBART로 추론하고 없으면 규칙 스텁으로 폴백하도록 바꿔, 학습한 모델 폴더만 지정하면 서빙이 교체된다.

**디버깅 — 무한 반복.** 첫 학습 후 평가에서 모델이 번역은 정확히 하면서도(`쫌→조금` 등 다 맞힘) 정답 뒤에 같은 구절을 끝없이 반복해 CER이 7.97까지 치솟았다. `repetition_penalty`·`no_repeat_ngram_size`로 완화하니 3.22로 줄었지만 근본 해결이 아니었다. `AutoTokenizer`로 KoBART 토크나이저의 실제 출력을 찍어 보니 **타깃 시퀀스에 EOS가 붙지 않았다**(KoBART 토크나이저는 자동으로 EOS를 넣지 않음). 즉 모델에게 "문장을 여기서 끝낸다"를 한 번도 가르치지 않은 것이 원인이었다. 전처리에서 라벨 끝에 `eos_token_id`를 추가하고 재학습하자 반복이 사라졌다.

**결과.** 재학습 후 val 500개에서 copy 기준선 대비 CER **4.62% → 1.01%**(글자 오류 약 78% 감소), 정확일치 **29.6% → 82.2%**로 크게 개선됐다. 아무것도 하지 않는 기준선을 학습 모델이 확실히 이겨, "표준이 못하는 것을 데이터로 개선한다"를 정량으로 증명했다. "증상(반복) → 임시 완화 → 실제 토큰 확인으로 근본 원인(EOS 누락) 발견 → 수정"의 과정으로 근본 원인을 짚었다. 이후 4지역 통합 학습셋(train 193만)으로 재학습해 copy 대비 CER 5.79%→1.63%, 정확일치 26.5%→77.7%로 4지역 변환을 완성했다.

## B-10. Whisper 파인튜닝과 표준 대비 개선 측정

**로컬 GPU로 전환.** 음성 학습은 Colab 대신 로컬 RTX 3080에서 하기로 했다. Colab GPU 한도·지오블록에서 자유롭고, 스트리밍 전처리기로 28GB zip을 안 풀고 클립만 뽑아 로컬 34GB 안에서 처리할 수 있었기 때문이다.

**base → small.** 경상도 Validation 음성 2만 클립으로 파인튜닝한 결과, 표준 대비 CER이 whisper-base **18.6%→10.2%(45% 개선)**, 이어 whisper-small은 **14.6%→6.7%(54% 개선)**로 base 절반 수준까지 좁혔다(라이브 테스트에서 base가 짧은 발화를 흘리던 문제가 있어 small로 교체). 변환 모델(KoBART)에 이어 **"표준이 못하는 영역을 데이터로 개선"을 두 번째로 정량 증명**한 것이다.

**배치 설계.** small은 10GB VRAM + 디스플레이 공유 때문에 micro 배치를 4로 낮추되 **그래디언트 누적 4로 effective 배치를 16**으로 만들어 base(batch 16)와 **동일한 유효 배치**로 학습했다. 이 덕에 6.7% vs 10.2% 차이가 배치 조건 차이가 아니라 **모델 크기(base→small) 효과**임이 분리된다. micro 배치는 순수 메모리 안전용, 누적은 학습 품질(수렴)용 — 배치 하나로 두 목적을 동시에 만족시킨 셈이다. (batch 8·16 시도는 cuBLAS 오류·디스플레이 드라이버 크래시로 실패했고, 타 앱의 VRAM 점유가 겹친 것이 원인이었다.)

이후의 4지역 확장·LoRA·소음 증강·충청 보강·데이터 확대 등 정확도 개선 반복은 B-11에 별도로 정리한다.

## B-11. STT 정확도 개선 반복 — 의사결정과 고민

4지역 STT를 만든 뒤, "제약(모델·용량 고정) 안에서 정확도를 어떻게 올릴까"를 반복 실험하며 각 단계의 근거를 남긴다.

**full 파인튜닝 대신 LoRA.** 표준어 발화를 사투리로 뭉개지 않으려면 원본 Whisper의 일반 능력을 보존해야 한다고 봤다. LoRA로 원본을 동결하니 2지역 검증에서 full(6.7%) vs LoRA(7.4%)로 근접했고, **표준어 보존 + 14MB 어댑터(온디바이스·지역확장 유리)** 이득이 커 LoRA를 택했다.

**나눠 학습(어댑터 이어학습).** 4지역 전체를 한 번에 돌리자 HF datasets의 특징 map 캐시가 디스크(52GB)를 넘겨 크래시했다. 이를 청크로 나눠 `--resume-adapter`로 이어 학습하니 세션당 캐시가 작아져 회피됐고, 나눠서 돌릴 유연성도 얻었다. 다만 후반 청크에서 clean이 미세하게 드리프트하는 걸 관찰해, **검증 성능 기준 최적 체크포인트를 선택**(백업→나쁘면 복원)하는 규칙을 세웠다.

**정직한 평가.** 처음 400표본에서 9.3%로 나온 CER이 held-out 전체 val에선 11.7%였다(특히 강원 뒤쪽 세션이 어려움). 표본이 아니라 전량으로 재서 낙관 편향을 제거했고, 이후 모든 판단을 전체 val + 실환경(소음 합성) + E2E(음성→표준어)로 했다.

**증강은 on-the-fly로.** 소음 증강을 콜레이터에서 배치마다 수행하니 map 캐시가 아예 안 생겨 디스크 문제가 원천 차단됐고, 매 에폭 다른 소음을 봐 효과도 좋았다. beam search는 재학습 없이 CER −0.72%p라 바로 채택했다.

**충청 보강 — forgetting 관리.** 최약 지역(충청)만 학습하면 다른 지역이 떨어질까 우려해, 충청 비중을 높이되 4지역 균형 rehearsal을 섞었다. 결과는 4지역 전부 개선(타 지역 저하 0) — 균형 rehearsal이 망각을 막는다는 걸 정량 확인했다.

**데이터 확대 vs 소음.** 확대 균형셋 + 실제소음(MUSAN) 1에폭은 clean을 더 낮췄지만(데이터 효과) 소음은 거의 불변이었다. 여기서 "소음 병목은 에폭 수가 아니다"라고 판단했다 — 1에폭에서 이미 flat이면 에폭을 늘려도 소음 이득이 작을 것이고, 병목은 **증강의 강도·다양성**이라고 봤다. 그래서 단순 2에폭 대신 **잔향(합성 RIR) + SpecAugment + harder SNR(−5~20dB)** 로 증강을 강화해 다시 돌렸다. 결과는 소음 −0.6%p(E2E 19.62→19.02%, 충청 29→27%) 개선에 clean +0.38%p 저하라는 트레이드오프였고, 실환경 우선으로 채택했다. 이 모든 변경은 학습 단계 전용이라 모델·용량·배포엔 영향이 없다(제약 준수).

여기서 얻은 결론은 **whisper-small은 소음에서 ~19%가 사실상 천장**이라는 점이다. LoRA·on-the-fly 증강·beam·충청 보강·데이터 확대·증강 강화까지 제약(모델·용량 고정) 안의 레버를 다 써도 소음 E2E가 19~20%에서 맴돌았고, 매 시도의 이득이 ~0.5%p로 줄며 clean 비용을 동반했다. 정확도가 수확 체감 구간에 든 만큼, 다음 우선순위는 정확도 추구가 아니라 배포(온디바이스 네이티브 통합·릴리스 요건)로 넘어가는 것으로 정리했다.

**충청 재보강 여부 — "느낌" 대신 데이터로 기각.** 충청은 4지역 중 계속 최약(clean E2E ~9.4%, 소음 27%)이라, 한 번 더 보강할지 고민했다. 이미 충청 +2만 클립 보강을 한 차례 했고 그때 충청 clean 이득이 −0.5%p로 작았기에, "더 넣어도 오를까"를 재학습 전에 **저비용으로 먼저 진단**하기로 했다. 재학습은 비용이 크고(삭제한 충청 데이터 재다운로드 + GPU 시간) 결과가 불확실하니, 서빙과 동일한 디코딩(beam=5)으로 충청 val 300발화를 돌려 **오류를 유형별로 분류하고 반복 치환 패턴을 집계**하는 분석([`training/analyze_cc_errors.py`](training/analyze_cc_errors.py))만 먼저 했다.

진단 결과(CER 9.48%)는 세 가지를 말해 줬다. 첫째, **오류가 산발적**이다 — 치환 492종 중 97%가 1회성이고 2회 이상 반복은 16종뿐. 특정 어휘·표현에 실수가 몰려 있지 않으니, 같은 성격의 데이터를 더 넣어도 **겨냥할 표적이 없다**(전형적인 음향 한계 신호). 둘째, 그 몇 안 되는 반복 오류마저 **실질 오류가 아니라 표기 규약 차이**였다 — 것→거(10회)·좀→쫌(4회) 같은 구어체 표기, 한번도→한 번도·해야되고→해야 되고 같은 띄어쓰기. 이는 재학습이 아니라 후처리 정규화로 해결되고, 애초에 후단 KoBART가 표준어로 정규화하며 대부분 흡수한다(즉 충청의 9.48%는 표기 규약 탓에 부풀려진 값이고 E2E 체감은 더 낫다). 셋째, 순수 실질 오류(어휘·구조)는 22%로 다양·랜덤한 음향 혼동(한참/한창, 정해져/전해져)이라 whisper-small 용량 한계에 해당한다.

결론은 **충청 재보강은 기각**이다. 남은 오류는 (a) 데이터가 아니라 정규화로 다룰 표기 규약과 (b) 모델 용량이 좌우하는 산발적 음향 혼동이 대부분이라, 같은 데이터를 더 넣는 것의 기대이득이 낮다. 충청은 "가장 약하지만 실사용 가능" 수준으로 두고, 남은 표기성 손해는 후처리·KoBART가 흡수하도록 맡긴 채 배포로 넘어가기로 했다.

## B-12. 평가 방법론과 온디바이스 검증의 의사결정

**컴포넌트 평가 → End-to-End.** STT(CER)와 변환(copy 대비)을 따로만 재다가, "사용자가 실제로 체감하는 정확도"는 음성→표준어 전체를 거친 결과라는 점에서 E2E 평가가 필요하다고 판단했다. 문제는 STT val 클립에 사투리 전사만 저장해 표준어 정답이 없던 것(라벨도 이미 삭제). 처음엔 변환기 MT 데이터의 {사투리:표준} 사전으로 역참조했으나, MT와 STT 오디오가 서로 다른 세션이라 커버리지가 2~200개로 낮아 실패했다. 그래서 라벨을 다시 받아 **클립의 stem+발화index로 표준어(standard_form)를 직접 매칭**하니 4지역 100% 매칭이 됐다. 또 "평가는 실제 서빙 조건과 일치해야 한다"는 원칙으로 E2E의 STT 디코딩을 서빙과 동일한 beam으로 맞췄고, clean뿐 아니라 소음 합성(phone+SNR10 등)까지 재 실환경 지표를 확보했다.

**온디바이스는 "검증 먼저, 통합 나중".** 네이티브 통합(whisper.cpp/onnxruntime + 커스텀 EAS 빌드)은 호흡이 긴 작업이라, 그 전에 "양자화해도 정확도가 버티나 + 용량이 얼마인가"를 먼저 확인하기로 했다. 실제 배포 경로인 CTranslate2 int8 변환은 ct2-transformers-converter가 transformers 4.47과 `dtype` 인자에서 충돌해 실패했고, whisper.cpp는 C 빌드가 필요해, **torch 동적 int8을 보수적 프록시**로 써서 검증했다(STT +0.25%p, KoBART +0.00%p, 총 ~370MB). 이 순서 덕에 "온디바이스가 현실적인가"를 큰 통합 작업 전에 저비용으로 판가름냈다.

## B-13. 데이터 라이선스·배포 판단

**무료 앱도 라이선스는 따로.** "무료면 괜찮겠지"가 아니라, 공개 배포는 라이선스상 상업/배포로 볼 수 있어 AI 허브에 1:1 문의로 "학습한 모델을 앱에 넣어 공개 배포해도 되는지"를 직접 확인했다. 이때 **원본 데이터가 아니라 학습 결과물(모델)을 배포한다**는 구분을 명확히 물었다. 답변은 파인튜닝 학습 결과물(모델)은 영리·비영리 배포 가능, 단 원본/추출 데이터(음성·전사·클립)는 재배포 금지, 출처 표기 필요였다. 이에 맞춰 (1) 클립·라벨은 저장소·앱 미포함(gitignore) 유지, (2) EVALUATION.md의 원문 전사 예시를 오류 유형 설명으로 대체, (3) README에 "AI 허브" 출처 고지를 추가했다.

**경쟁력 포지셔닝.** 범용 STT(구글·네이버·카카오)와 정확도 정면승부는 열세임을 인정하고, 오프라인·프라이버시·사투리 번역 특화라는 니치에서의 경쟁력에 집중하기로 했다. 그 니치를 강화하는 방향으로 실환경(소음) 정확도 개선과 온디바이스 검증을 우선순위로 두되, "완성된 앱"을 위해서는 정확도 무한 추구보다 배포(네이티브 통합·릴리스 요건)로 넘어가야 한다는 균형도 기록해 둔다.

## B-14. 아키텍처 선택 — 단일 병합 모델 vs 지역별 LoRA 어댑터

로드맵의 "공유 백본 + 지역별 LoRA 어댑터"(base 동결 + 지역마다 별도 어댑터 + 추론 시 라우팅)와 현재의 "4지역 단일 병합 모델" 중 무엇을 계속 가져갈지 고민했다. 결론은 **단일 병합 모델 유지**다.

근거: (1) 실험적으로 **교차지역 데이터가 서로 도움**이 됐다 — 충청 보강이 경상까지 개선했고, 데이터 확대가 전 지역을 낮췄다. 사투리들은 공유 패턴이 커서 합쳐 학습하는 편이 유리했다. 지역별로 쪼개면 이 교차 이득을 잃는다. (2) 지역별은 **추론 라우팅**(앱 지역 선택 또는 분류기)이 필요하고, 잘못 라우팅하면 오히려 악화되는 실패모드가 생긴다. 단일 모델은 라우팅이 없어 단순하다. (3) 지역별의 유일한 확실한 이점인 "지역 추가 편의"도, 단일 모델을 **균형 rehearsal로 재학습하면 forgetting 없이** 되는 걸 이미 확인했다(제주도 같은 방식).

지역별 어댑터는 **정확도 우위가 아니라 확장성·온디바이스 모듈성**이 강점이므로, 지역이 계속 늘고 온디바이스에서 어댑터 스왑이 필요해질 때의 선택지로 남긴다. 중요한 점은 그때 가더라도 base·데이터·증강·평가·라이선스·양자화 검증을 **전부 재활용**하고 어댑터 분리 + 라우팅만 추가하면 되므로, 지금까지의 작업은 어느 경로에서도 버려지지 않는다.

## B-15. 배포 준비 — 아키텍처 결정과 무관한 것부터

정확도가 천장에 닿아 배포 단계로 넘어가면서, 먼저 **온디바이스로 갈지 서버로 갈지의 큰 갈림길과 상관없이 어느 쪽에서도 반드시 필요한 준비**부터 했다. 큰 결정(뒤에서 서술)은 비용·오프라인·공수의 트레이드오프가 커 사용자 판단이 필요하지만, 릴리스 설정·라이선스 고지·개인정보처리방침은 공통 선행 작업이라 지금 확정해 두면 이후 어느 경로로 가든 그대로 쓰인다.

구체적으로 (1) `eas.json`에 dev/preview/production 빌드 프로파일을 만들고(production은 Play Store용 app-bundle + versionCode 자동 증가), (2) `app.json`에 릴리스 식별자(`android.package`·`versionCode`, iOS `bundleIdentifier` = `com.ysb2152.saturitranslator`)를 넣었다. (3) 라이선스상 필수인 **AI 허브 출처 표기**를 앱 화면 하단에 고지로 넣었다(B-13의 이용정책 준수를 코드로 반영). (4) **개인정보처리방침 초안**([docs/privacy-policy.md](docs/privacy-policy.md))을 작성했는데, 문구를 실제 동작과 일치시키는 걸 원칙으로 삼아 백엔드를 확인했다 — 업로드 음성을 임시파일로 처리한 뒤 `finally`에서 즉시 삭제하고 DB에 남기지 않으므로, "음성을 저장하지 않고 처리 직후 삭제"로 정확히 기술했다. 앱의 데모(샘플) 버튼은 이전에 이미 `__DEV__` 게이팅해 릴리스 빌드에선 노출되지 않는다.

남은 큰 결정은 **온디바이스 vs 서버**다. 온디바이스(whisper.cpp/onnxruntime + 커스텀 EAS 네이티브 빌드)는 서버 비용이 없고 오프라인·프라이버시에 유리하지만 네이티브 통합 공수가 크다 — 다만 B-12에서 **양자화해도 정확도가 유지되고 총 ~370MB로 담긴다**는 실현가능성은 이미 확인해 뒀다. 서버 방식(호스팅 백엔드)은 공수가 작고 빠르게 실제 링크를 얻지만 상시 과금 또는 상시 가동 PC가 필요하다(WTGMate에서 Cloudflare Tunnel로 다뤄 본 트레이드오프와 같은 성격). 이 선택은 다음 단계에서 확정한다. 공통 준비를 먼저 끝낸 덕에, 어느 쪽을 고르든 설정·고지·방침을 다시 만들 필요는 없다.

**버전 호환 스파이크 — 한글 경로가 진짜 블로커였다.** 온디바이스 통합의 1순위 리스크로 "RN 0.86 + React 19 + New Architecture라는 최신 조합에서 네이티브 빌드가 되는가"를 꼽았고(계획서 §7-1), 무거운 네이티브 모듈을 붙이기 전에 **빈 dev client 빌드**부터 검증했다. `expo-dev-client` 설치 → `expo prebuild`(android 생성, `newArchEnabled=true` 확인 — SDK 57 기본값) → `gradlew assembleDebug`. 그런데 빌드가 19초 만에 `Error resolving plugin [com.facebook.react.settings] … Included build … does not exist`로 실패했고, 에러 로그의 경로가 깨져 나왔다(`뭐라?���?`). gradle-plugin은 실제로 존재했으므로 원인은 누락이 아니라 **한글 프로젝트 경로(`C:\Users\ysb21\뭐라는겨`)의 인코딩**이었다. junction으로 ASCII 경로를 만들어도 gradle이 실제 경로(한글)로 되돌려 해석해 동일 실패 — 즉 링크로는 못 피하고 실제 파일이 ASCII 경로에 있어야 했다. 프로젝트를 ASCII 경로로 복사해 다시 돌리니 `BUILD SUCCESSFUL`. **결론: SDK 57/RN 0.86/New Architecture 자체는 문제없고, 한글 폴더명만이 유일한 안드로이드 빌드 블로커였다.** 조치는 로컬 프로젝트 폴더를 영문 경로로 이전하는 것(깃헙 원격 저장소 이름·소스는 무관, venv는 `python.exe` 직접 호출 기준 동작 유지). 리스크가 큰 최신-버전 호환은 오히려 깨끗했고, 실제 함정은 경로 인코딩이라는 사소하지만 치명적인 곳에 있었다.

조치 후 확인: 폴더를 `뭐라는겨`→`saturi-translator`(영문)로 이전하고, 리포·모델 아티팩트·venv가 온전한지 점검(git 원격 동기화 유지, venv는 `python.exe` 직접 호출로 정상 동작)한 뒤, `expo prebuild --clean`으로 android를 새 경로에 재생성하고 `gradlew assembleDebug`를 돌리니 **`BUILD SUCCESSFUL`(427 태스크, `app-debug.apk` 171.8MB)**. 네이티브 C/C++ 컴파일(react-android 0.86.3, expo-modules-core)까지 포함해 통과했다. 이로써 온디바이스 통합의 1순위 리스크(최신 버전 조합의 네이티브 빌드)가 해소됐고, 다음은 이 dev client에 whisper.rn을 붙이는 것이다.

**whisper.rn 통합과 온디바이스 STT 실기 검증.** whisper.rn 0.7.4를 설치해 리빌드하니 네이티브 whisper.cpp가 RN0.86에서 그대로 컴파일됐고(APK에 x86_64 포함이라 에뮬레이터에서도 동작), STT 래퍼([`mobile/src/ondeviceStt.js`](mobile/src/ondeviceStt.js))를 붙였다. 실기 검증에서 몇 가지 함정을 순서대로 풀었다: (1) Metro 번들이 whisper.rn→safe-buffer→`buffer`(Node 내장) 미해결로 실패 → `buffer` 패키지 설치로 폴리필. (2) initWhisper가 "Failed to load the model" → adb로 앱 외부저장소에 push한 파일을 앱 uid가 못 읽는 권한 문제였고, `adb root` 후 앱 **내부** files 디렉터리에 넣고 소유권을 앱 uid로 바꿔 해결. (3) 그 `adb root`가 adbd를 재시작하며 `adb reverse tcp:8081` 포워딩을 지워 dev client가 metro에 접속 불가 → reverse 재설정. 결과, 우리 f16 모델이 에뮬레이터에서 샘플 음성을 **"이날치라고 그 국악이 가미된 밴드"** 로 인식했고, 정답과 조사 한 글자(에→이)만 달랐다 — 서버와 동일한 정확도다. 소요 34초는 에뮬 x86_64 CPU + f16 탓으로, 실기기(arm)와 q5 양자화에서 크게 줄 것이다. 온디바이스 STT가 빌드·로드·인식까지 기기에서 end-to-end로 증명됐다.

---

# Part C. 배운 것과 로드맵

## C-1. 개발하며 굳어진 관점

학습보다 먼저 파이프라인 뼈대를 관통시키면, 데이터가 준비되기 전에도 "돌아가는 데모"를 확보하고 이후 개선 전/후를 비교할 기준선을 얻는다. 엔진 선택은 환경 제약에서 출발했다(ffmpeg 부재 → faster-whisper). 개발 환경 구축도 필요 이상으로 무겁게 가지 않고(커맨드라인 도구), 가정을 실제로 점검했다(WHPX 가속 확인 후 불필요한 관리자 단계 생략). 프레임워크 버전이 바뀌면 관례가 깨질 수 있어(SDK 57의 FormData 파일 파트 폐지) 버전 고정 문서를 먼저 확인하는 것이 필요했다. 정확도 개선 단계에서는 "정직한 전량 평가", "실제 서빙 조건과 일치하는 측정", "제약(모델·용량 고정) 안에서의 레버 선택"이 원칙으로 굳어졌다.

## C-2. 앞으로의 로드맵

- [x] 기획 정리와 기술 스택 확정 (Part 0)
- [x] 백엔드 STT+변환 파이프라인 API 뼈대 (B-1)
- [x] 앱 녹음 → 전송 → 결과 뼈대 (B-2)
- [x] 개발용 안드로이드 에뮬레이터 환경 (B-3)
- [x] 업로드 경로 버그 해결 (B-4)
- [x] 디자인 방향 확정과 1차 구현 (B-5)
- [x] GitHub 공개 저장소 구성 (B-6)
- [x] AI Hub 방언 데이터 전처리 스크립트·테스트 (B-7)
- [x] 실제 라벨로 변환 문장쌍 209만 생성 + 균형 학습셋 45.5만 (B-7, B-9)
- [x] KoBART 변환 모델 4지역 파인튜닝 (B-9) — copy 대비 CER 5.79%→1.63%
- [x] Whisper STT 4지역 LoRA 파인튜닝 + 소음 증강 + 충청 보강 (B-10, B-11) — 표준 대비 CER 20.3%→11.7%
- [x] 파인튜닝 Whisper·KoBART 백엔드 서빙 연결 — 앱에서 STT→변환 전체 파이프라인 동작 확인
- [x] End-to-End·실환경·온디바이스 양자화 정량 평가 (B-12) — `EVALUATION.md`
- [x] 데이터 라이선스 확인 + 출처 표기 (B-13)
- [x] 실환경(소음) 정확도 추가 개선 (증강 강화, B-11) — 소음 E2E 19.6→19.0%, whisper-small 소음 ~19% 천장 확인 → 정확도 수확체감
- [x] 배포 공통 준비 (B-15) — eas.json 빌드 프로파일·app.json 릴리스 식별자·AI허브 출처 표기·개인정보처리방침 초안
- [x] **배포 방식 결정: 온디바이스 채택** (서버 대비 비용0·오프라인·프라이버시, 양자화 실현가능성 기검증) — 통합 계획: [docs/ondevice-plan.md](docs/ondevice-plan.md)
- [ ] 온디바이스 통합 — STT(whisper.rn+GGUF) → 변환기(onnxruntime-react-native+ONNX, 토크나이저/생성루프가 핵심 리스크) → 첫실행 모델 다운로드 (계획서 §7 실행순서)
- [ ] 플레이스토어 배포 — 개발자 계정·개인정보처리방침 URL 호스팅·EAS Build(dev client→AAB)·스토어 등록
- [ ] (확장) 제주도 등 지역 추가, 필요 시 공유 백본 + 지역별 LoRA 어댑터 구조 (B-14)

---

문서 관리: 기능이나 방향이 바뀌면 이 파일도 함께 수정하고, 상단 "0. 업데이트 이력"에 한 줄 추가한다. 결과뿐 아니라 그때의 고민·의사결정 근거를 함께 남긴다.
