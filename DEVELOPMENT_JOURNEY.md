# 사투리 번역기 개발 기록

이 문서는 코드와 함께 관리하는 개발 기록입니다. 기능을 만들 때마다 무엇을 고민했고 왜 그렇게 정했는지, 어떤 문제를 만나 어떻게 풀었는지를 남깁니다. 나중에 포트폴리오나 면접에서 바로 쓸 수 있게 하는 것이 목적입니다. 처음 설계는 Part 0에, 이후 바꿔 나간 과정은 Part B에 정리합니다. (프로젝트 배경의 자세한 맥락은 [CLAUDE_1.md](CLAUDE_1.md) 참고.)

- 프로젝트: 사투리 → 표준어 음성 번역 안드로이드 앱. 사투리 음성을 녹음하면 표준어 텍스트로 바꿔 준다.
- 스택: React Native(Expo) 프론트, FastAPI + Python 백엔드, faster-whisper(STT), KoBART/T5 계열(사투리→표준어 변환, 예정), AI Hub 한국어 방언 데이터, Colab/Kaggle 무료 GPU 학습.
- 저장소: https://github.com/ysb2152/translate (public, 임시명 `translate`. backend/mobile 모노 구조).

---

## 0. 업데이트 이력 (최신순)

| 시점 | 구분 | 한 일 | 관련 |
|---|---|---|---|
| 2026-08-28 | feat | 용량 문제 대응: Colab 노트북(`notebooks/aihub_colab.ipynb`) 작성 — aihubshell로 **부분 다운로드** → 전처리 → Drive 저장. 로컬(34GB) 대신 학습 환경에서 소량만 처리 | B-8 |
| 2026-08-28 | feat | AI Hub 방언 데이터 전처리 파이프라인 작성(`data/`) — 라벨 JSON → STT 매니페스트 + (방언→표준) 문장쌍, 오디오 구간 슬라이스, 합성 샘플로 E2E 테스트 통과 | B-7 |
| 2026-08-28 | chore | GitHub 공개 저장소 `translate` 생성·푸시(WTGMate처럼 코드와 함께 개발기록 관리). Expo 내부 .git이 서브모듈로 잡히던 문제 정리 | B-6 |
| 2026-08-28 | design | 스타일 5방향 탐색 후 **"한지 × 클린 하이브리드"** 채택, `App.js` 재구현(한지빛 배경·먹색 세리프·청록 포인트·View 마이크·클린 결과카드) | B-5 |
| 2026-08-28 | chore | 범위 확장 반영: 앱 카피에서 "경상도" 제거 → **전국 사투리** 대상으로 일반화 | B-5 |
| 2026-08-28 | fix | 업로드 실패(`unsupported FormDataPart`) 원인 규명 후 `expo-file-system`의 `File.upload`(multipart)로 해결, 왕복 실측 확인 | B-4 |
| 2026-08-28 | chore | 안드로이드 에뮬레이터를 커맨드라인 도구로 구축(JDK17·SDK·AVD `saturi_pixel6`), WHPX 가속 확인 | B-3 |
| 2026-08-28 | feat | 앱 뼈대(Expo SDK 57, `expo-audio` 녹음 → `/transcribe` 호출 → 결과 화면) | B-2 |
| 2026-08-28 | feat | 백엔드 E2E 뼈대(FastAPI `/transcribe`·`/health`, faster-whisper STT, 규칙 스텁 변환기) | B-1 |
| 2026-08-28 | init | 기획 정리 및 기술 결정(React Native/Expo, faster-whisper 확정) | Part 0 |

지금까지는 학습(모델) 이전 단계로, **파이프라인이 앱에서 끝까지 흐르는 뼈대를 먼저 관통시키고 디자인 방향을 확정**한 상태입니다. STT는 파인튜닝 전 기본 Whisper, 사투리→표준어 변환은 규칙 사전 스텁으로, 둘 다 이후 학습된 모델로 교체할 자리만 잡아 둔 것입니다. 프로젝트의 핵심 서사인 "표준 모델이 못하는 사투리를 데이터로 개선한다"는 다음 단계에서 채웁니다.

---

# Part 0. 프로젝트의 출발점

처음 이 프로젝트를 구상했을 때의 설계와 계획입니다. 이후의 변화는 모두 여기에서 출발합니다.

**처음의 목표와 형태.** 취업 포트폴리오로 만드는 두 번째 프로젝트입니다(첫 번째는 RouteMate/WTGMate). 이번에는 "모델을 직접 학습시킨 경험"과 "앱을 배포한 경험"을 동시에 채우는 것을 목표로, 플레이스토어에 실제 출시하는 안드로이드 앱을 잡았습니다. 부모·조부모 세대의 사투리를 잘 못 알아듣는 자녀 세대, 사투리 콘텐츠의 자막화·고객센터 같은 실사용 시나리오로 이야기를 세웠습니다.

**처음의 기술 선택.** 아키텍처는 서버 기반으로 정했습니다. 앱은 음성을 FastAPI 서버로 보내고 결과만 받아오며, 온디바이스 추론(ONNX/TFLite 변환·양자화)은 지금 범위에서 제외하고 이후 심화 확장으로 남겼습니다. 파이프라인은 두 단계로, 1단계 STT가 사투리 음성을 사투리 텍스트로, 2단계 변환이 사투리 텍스트를 표준어 텍스트로 바꿉니다. STT는 OpenAI Whisper의 작은 버전을 방언 데이터로 파인튜닝하고, 변환은 KoBART나 소형 T5를 seq2seq로 파인튜닝하는 계획이었습니다. 데이터는 AI Hub의 한국어 방언 발화 데이터를 쓰기로 했는데, 음성 + 사투리 전사 + 표준어 대응이 세트라 STT와 변환 두 모델을 하나의 데이터로 학습할 수 있기 때문입니다. 학습은 Colab/Kaggle 무료 GPU, 백엔드는 RouteMate와 동일한 FastAPI 패턴을 재사용합니다.

**처음부터 명확히 하려던 원칙.** 이 프로젝트의 정체성은 알고리즘이 아니라 **데이터로 성능을 개선하는 방법론**입니다. 지원자가 ETRI 객체인식 프로젝트에서 특정 객체의 인식률이 낮은 원인을 데이터에서 찾아 보강·재학습으로 개선한 경험이 있고, 그 흐름을 "표준 Whisper가 사투리를 못 알아듣는 원인을 분석하고 데이터로 개선한다"로 그대로 재현하는 것이 핵심 스토리입니다.

**당시의 한계(미정 항목).** 앱 프론트엔드 프레임워크 미확정, AI Hub 데이터의 정확한 다운로드·전처리 절차 미확인, 두 모델을 하나의 파이프라인으로 잇는 API 설계, 플레이스토어 출시 요건(개발자 계정·개인정보처리방침)이 처음의 열린 문제였습니다. 시작 지역도 처음에는 데이터가 풍부하고 억양이 뚜렷한 경상도로 잡았습니다.

---

# Part A. 프로젝트 개요와 현재 설계

**풀려는 문제.** 표준 음성인식 모델은 사투리 발음·억양·어휘에서 인식률이 떨어집니다. 그 격차를 방언 데이터로 좁혀, 사투리 음성을 표준어 텍스트로 정확히 옮기는 것이 목표입니다.

**사용자 흐름.** 앱에서 녹음 버튼을 눌러 사투리로 말하면, 녹음 파일이 서버로 업로드되고, 서버가 STT로 사투리 텍스트를 뽑은 뒤 변환 모델로 표준어 텍스트를 만들어 돌려줍니다. 앱은 "인식된 사투리"와 "표준어" 두 결과를 함께 보여 줍니다.

**파이프라인.**

| 단계 | 입력 → 출력 | 담당 | 현재 상태 |
|---|---|---|---|
| 1. STT | 사투리 음성 → 사투리 텍스트 | faster-whisper | 기본 `base`(파인튜닝 전) |
| 2. 변환 | 사투리 텍스트 → 표준어 텍스트 | KoBART/T5(예정) | 규칙 사전 스텁 |
| (선택) TTS | 표준어 텍스트 → 음성 | 미정 | 미착수 |

**확정된 방향.** 프론트엔드는 React Native(Expo)로 정했습니다(B-2). STT 추론 엔진은 faster-whisper로 정했습니다(B-1). 범위는 경상도 한정이 아니라 AI Hub가 제공하는 전 지역 방언을 목표로 넓혔고, 그에 맞춰 앱 문구도 "전국 사투리" 기준으로 바꿨습니다(B-5). 디자인은 "한지 × 클린 하이브리드"로, 정체성은 한지 정서(한지빛 배경·먹색 세리프·청록/주칠), 작업 화면은 토스식 명료함(깔끔한 카드·또렷한 위계)으로 잡았습니다(B-5).

**실패를 고려한 설계.** 앱은 서버 주소를 화면에서 바꿀 수 있게 해 에뮬레이터(`10.0.2.2`)와 실기기(PC의 LAN IP)를 모두 지원합니다. 백엔드는 업로드 확장자를 검증하고(415), 파일 크기 상한과 빈 파일을 막으며, STT 실패 시에도 서버가 죽지 않게 예외를 감쌉니다. 결과가 비어도(무음 등) 화면이 멈추지 않고 안내 문구를 보여 줍니다.

---

# Part B. 개발 진행 로그

여기부터가 실제로 손을 대며 만들어 간 기록입니다. 각 항목은 무엇이 문제였고 어떤 선택을 했으며 왜 그렇게 했는지, 무엇이 어려웠는지를 남깁니다.

## B-1. 백엔드 E2E 뼈대와 STT 엔진 선택

학습을 먼저 하지 않고, **파이프라인 전체가 도는 뼈대부터 관통**시키기로 했습니다. 포트폴리오 ML 프로젝트가 데이터·학습에만 몇 주 매달리다 앱은 껍데기도 못 만드는 실패를 피하려는 판단이었습니다. 그래서 STT는 파인튜닝 전 기본 Whisper, 사투리→표준어 변환은 단어 사전 규칙 스텁으로 두고, 녹음 → STT → 변환 → 결과가 끝까지 흐르는지를 먼저 확인했습니다. 두 모델 자리는 시그니처만 고정해 두어, 이후 학습된 모델로 이 자리만 교체하면 됩니다.

STT 추론 엔진은 faster-whisper(CTranslate2)로 정했습니다. 개발 PC에 시스템 ffmpeg가 없었는데, 표준 openai-whisper는 오디오 디코딩에 ffmpeg가 필요한 반면 faster-whisper는 내부에 디코더(PyAV)를 번들해 시스템 ffmpeg 없이 동작하고, CPU에서도 더 빠릅니다. 파인튜닝한 모델은 CTranslate2로 변환해 `WHISPER_MODEL_DIR` 환경변수로 연결하도록 자리를 잡았습니다.

백엔드는 FastAPI로 `POST /transcribe`(오디오 업로드), `GET /health`를 만들고, 모델은 첫 요청 지연을 피하려 기동 시 예열합니다. 스텁 변환기(`DialectConverter`)는 "가가→그 아이가", "억수로→굉장히" 같은 최소 사전으로만 동작하며, 2단계에서 KoBART/T5 추론으로 이 클래스만 교체하면 되도록 인터페이스를 유지했습니다.

사소한 함정으로, faster-whisper가 `requests`를 런타임에 요구하는데 의존성으로 딸려오지 않아 임포트가 실패했습니다. `requirements.txt`에 명시해 해결했습니다. 톤 신호 WAV로 배관을 확인했을 때 텍스트는 비었지만(음성이 아니라 VAD가 걸러냄) 모델 로딩·추론·길이 계산이 정상 동작하는 것을 확인했습니다.

## B-2. 앱 뼈대와 프레임워크 선택

앱 프레임워크는 React Native(Expo)로 확정했습니다. 후보는 React Native / Flutter / 네이티브(Kotlin)였는데, 첫 프로젝트(RouteMate)가 Electron이라 이미 React/JS 경험이 있어 학습 곡선이 가장 완만하고, 이 앱의 로직이 "녹음 → 서버 전송 → 결과 표시"로 단순해 네이티브의 성능 이점이 필요 없다고 봤습니다. Expo의 `expo-av`/`expo-audio` 녹음과 EAS Build로 플레이스토어 빌드·서명까지 빠르게 도달할 수 있는 점도 이유였습니다.

프로젝트는 Expo SDK 57로 생성됐습니다. SDK 57의 `AGENTS.md`가 버전 고정 문서를 먼저 읽으라고 명시해, 녹음 API는 구식 `expo-av`가 아니라 SDK 57 권장인 `expo-audio`의 훅(`useAudioRecorder`, `useAudioRecorderState`, `RecordingPresets`)으로 구현했습니다. 마이크 권한 문구는 `app.json`의 `expo-audio` 플러그인 설정에 넣었습니다. 서버 주소는 앱 화면에서 수정 가능하게 해, 에뮬레이터와 실기기 환경 차이를 코드 수정 없이 흡수하도록 했습니다.

## B-3. 안드로이드 에뮬레이터를 커맨드라인으로 구축

앱을 실제로 돌려 볼 에뮬레이터가 필요했습니다. Android Studio 전체를 까는 대신 **커맨드라인 도구만** 설치하는 길을 택했습니다. 개발용 IDE가 지금은 필요 없고(Expo로 개발), 디스크 여유가 넉넉하지 않아(C: 약 34GB) 가벼운 쪽이 맞았기 때문입니다.

설치는 관리자 권한 없이 진행했습니다. JDK 17을 portable zip으로 풀고(sdkmanager/avdmanager가 Java 앱이라 필요), Android command-line tools를 `cmdline-tools/latest`에 배치한 뒤, `ANDROID_HOME` 등 환경변수를 사용자 범위로 영구 등록했습니다. sdkmanager로 platform-tools·emulator·`platforms;android-34`·`system-images;android-34;google_apis;x86_64`를 설치하고(라이선스는 stdin 파일 리다이렉션으로 일괄 수락), AVD `saturi_pixel6`(Pixel 6 / API 34)를 만들었습니다.

가장 걱정한 것은 하드웨어 가속이었습니다. CPU가 AMD(Ryzen 5 5600X)라 Intel HAXM을 못 쓰고 Windows Hypervisor Platform(WHPX)이 필요한데, `emulator -accel-check`로 확인하니 **WHPX가 이미 설치·사용가능** 상태여서 관리자 권한·재부팅 단계가 통째로 생략됐습니다. 콜드 부팅이 12~27초로 빨랐습니다. Expo Go를 에뮬레이터에 설치해 앱을 올리고, 녹음→업로드→변환 왕복까지 확인했습니다(무음 마이크라 텍스트는 Whisper 환각이 나오지만 배관은 정상). x86_64 이미지를 고른 것도 AMD 호스트에서 네이티브 가상화로 돌리기 위함이었습니다.

## B-4. 업로드 실패(unsupported FormDataPart) 해결

앱에서 녹음 후 변환하면 `unsupported FormDataPart implementation` 오류가 났습니다. 처음 구현은 React Native 관례대로 전역 `fetch`에 `FormData`를 만들고 `{ uri, name, type }` 형태의 파일 파트를 append하는 방식이었는데, Expo SDK 57(RN 0.86, New Architecture)에서는 이 구식 파일 파트가 더 이상 지원되지 않는 것이 원인이었습니다.

해결은 Expo 표준 업로드인 `expo-file-system`의 `File.upload`로 바꾸는 것이었습니다. SDK 57 문서를 확인해 새 File 클래스 API(`new File(uri).upload(url, { uploadType: UploadType.MULTIPART, fieldName, mimeType, httpMethod })`)가 권장 경로임을 파악하고, 설치된 패키지의 타입 정의에서 반환형(`{ body, status, headers }`)까지 직접 확인한 뒤 `src/api.js`를 재작성했습니다. `expo-file-system`은 Expo Go에 기본 내장이라 커스텀 빌드 없이 바로 동작합니다.

버그를 고친 뒤 에뮬레이터에서 녹음→업로드→변환을 눌러, 결과 카드에 텍스트와 길이(2.25초)가 뜨는 것으로 오류가 사라지고 서버 왕복이 되는 것을 실측했습니다. 새 패키지를 설치했으므로 실행 중이던 Metro가 이를 알도록 캐시를 비우고(`--clear`) 재시작해야 했던 점(그렇지 않으면 모듈을 못 찾음)도 확인했습니다.

## B-5. 디자인 방향 탐색과 한지 × 클린 하이브리드

뼈대가 도는 것을 확인한 뒤 앱의 비주얼을 잡았습니다. 처음에는 "유쾌한" 파스텔 버전을 만들었으나, 방향을 원점에서 다시 잡기로 하고 Canva·미리캔버스·Figma를 참고해 앱에 어울릴 스타일 5방향을 목업으로 비교했습니다(또랑또랑 클린 / 정겨운 한지 / 보이스 웨이브 / 말랑 파스텔 / 미니멀 에디토리얼). 각 방향을 실제 앱 화면 목업 + 팔레트 + 타이포 + "왜 어울리나"로 나란히 놓아 고르기 쉽게 했습니다.

선택은 **"한지 × 클린 하이브리드"**였습니다. 이 앱의 무기가 "표준 모델이 못하는 사투리를 데이터로 개선했다"는 서사라, 정체성은 한국적 정서(한지빛 배경·먹색 세리프 타이틀·단청에서 뽑은 청록/주칠)로 각인시키고, 녹음·결과 같은 작업 화면은 토스식 명료함(깔끔한 카드·또렷한 위계)으로 신뢰를 주는 조합이 차별화와 완성도를 동시에 잡는다고 판단했습니다. 청록 하나를 주 강조색으로 통일해 전통색이자 클린한 포인트로 동시에 읽히게 했고, 세리프(정체성)와 산세리프(기능)로 역할을 나눴습니다.

구현에서는 앱 용량·의존성을 아끼려 정확한 Gowun Batang 대신 안드로이드 기본 세리프(`fontFamily: 'serif'` → Noto Serif CJK)로 한글 세리프 느낌을 냈고, 마이크 아이콘도 이모지 대신 View로 그려 의존성 0으로 유지했습니다. 녹음 중에는 주칠색과 펄스 링으로 상태를 드러냅니다. 이와 함께 프로젝트 범위가 전국 사투리로 넓어진 것을 반영해, 앱 카피에서 "경상도"를 빼고 "전국 사투리"·"사투리로 말하면 표준어로" 기준으로 문구를 일반화했습니다.

## B-6. GitHub 저장소 구성

코드와 개발기록을 함께 관리하려고(WTGMate와 같은 방식) GitHub 공개 저장소 `translate`(임시명)를 만들었습니다. backend/mobile 모노 구조 그대로 올리고, 이후 이 `DEVELOPMENT_JOURNEY.md`를 커밋과 함께 갱신합니다.

첫 커밋에서 함정이 하나 있었습니다. `create-expo-app`이 `mobile/`을 만들 때 내부에 자체 `.git`을 초기화해 둬서, 루트에서 `git add`를 하면 `mobile`이 파일이 아니라 서브모듈(gitlink, mode 160000)로 잡혔습니다. 이대로 올리면 앱 소스가 빠집니다. `mobile/.git`을 제거하고 인덱스의 gitlink를 강제로 지운 뒤(`git rm --cached -f mobile`) 다시 추가해 앱 소스를 정상 포함시켰습니다. 무거운 산출물(node_modules, .venv, tmp, dist)은 루트와 하위 `.gitignore`로 제외했습니다.

## B-7. AI Hub 방언 데이터 전처리 파이프라인

학습에 앞서, AI Hub "한국어 방언 발화" 데이터를 학습용 산출물로 바꾸는 전처리를 만들었습니다(`data/`). 하나의 데이터에서 두 모델을 학습하는 것이 목표라 산출물도 둘로 나눴습니다. STT(Whisper) 파인튜닝용 매니페스트(`{audio_filepath, text, duration}`, text는 방언 전사)와, 변환 모델(KoBART/T5)용 문장쌍(`{dialect, standard}`)입니다.

가장 큰 제약은 **데이터를 직접 받을 수 없다는 것**이었습니다. AI Hub는 계정·승인·수동 다운로드가 필요합니다. 그래서 실제 데이터 없이도 개발·검증이 가능하도록, 라벨 구조를 흉내 낸 합성 샘플 생성기(`make_sample.py`)와 스모크 테스트(`test_preprocess.py`)를 함께 만들어, 정제·추출·엔드투엔드(샘플 생성 → 전처리 → 산출물·클립 존재 검증)가 한 번에 돌게 했습니다.

두 번째 제약은 **라벨 스키마가 데이터셋·연도·지역마다 조금씩 다르다는 것**이었습니다. 웹 조사로는 정확한 필드명을 확정할 수 없어, 특정 필드명에 못박지 않고 방어적으로 설계했습니다. `aihub_schema.py`의 `FIELDS`에 후보 필드명을 나열해 순차 탐색하고, 문장 필드(`dialect_form`/`standard_form`)가 없으면 어절 리스트(`eojeolList`)로 조립하며, 그것도 없으면 원문(`form`)으로 대체합니다. 전사에 섞이는 마커 — `(A)/(B)` 이중전사, `{웃음}`, `(())` 불명확 구간, 어절 뒤 `b/`·`l/` 같은 KsponSpeech 계열 표기 — 는 `clean_text`가 정제하되, 방언은 이중전사의 앞쪽, 표준은 뒤쪽을 고르도록 mode를 나눴습니다. 실제 파일을 받으면 라벨 하나만 열어 `FIELDS`를 맞추면 대부분 그대로 동작합니다.

오디오는 세션 단위 긴 WAV에 발화별 `start`/`end`가 붙는 경우를 기본으로 보고, 표준 라이브러리 `wave`만으로 구간을 잘라 발화 클립을 저장합니다(`wav_utils.slice_wav`). 이미 발화 단위로 잘린 데이터면 `--no-slice`로 원본을 참조합니다. train/val 분할은 발화가 아니라 **세션(라벨 파일) 단위**로 해서 같은 화자·세션이 두 셋에 걸쳐 새는 것을 막았습니다. 너무 짧거나(0.3s 미만) Whisper 한계(30s)를 넘는 발화는 제외하고, 방언과 표준이 동일한 쌍의 비율 등은 `stats.json`으로 남겨 데이터 품질을 눈으로 확인할 수 있게 했습니다.

## B-8. 데이터 용량 문제와 Colab 부분 다운로드

전처리 파이프라인은 준비됐지만 실제 데이터가 걸림돌이었습니다. AI Hub 방언 데이터는 지역당 3,000시간이 넘어 수백 GB라, 개발 PC의 여유 공간(약 34GB)에는 전량이 들어가지 않습니다. 그런데 포트폴리오의 목적은 "표준 대비 개선"을 보이는 것이라 전량이 필요하지 않다고 판단했습니다. 수십 시간, 혹은 그 이하로도 baseline 대비 개선을 측정할 수 있습니다.

그래서 두 가지로 우회했습니다. 첫째, AI Hub의 `aihubshell`이 파일 단위(`filekey`) **부분 다운로드**를 지원하므로 원하는 청크만 골라 받습니다. 둘째, 어차피 학습은 Colab 무료 GPU에서 하므로 데이터도 로컬이 아니라 **Colab에서 직접** 받아 전처리하면 로컬 디스크가 병목이 되지 않습니다. 이 흐름을 담은 노트북(`notebooks/aihub_colab.ipynb`)을 만들었습니다. aihubshell 설치 → 파일 목록·용량 확인 → 소량 부분 다운로드 → 압축 해제 → 저장소 clone 후 `data/preprocess.py` 실행 → 산출물을 Google Drive에 저장하는 순서이고, 디스크가 빠듯하면 "한 청크 받기 → 전처리 → 원본 삭제 → 다음"을 반복하는 증분 처리도 안내했습니다(전처리가 작은 클립+매니페스트만 남기므로 사용량이 낮게 유지됨).

## B-9. (예정) Whisper 파인튜닝과 표준 대비 개선 측정

데이터가 준비되면 표준 Whisper의 사투리 인식 오류를 먼저 측정하고(baseline), 방언 데이터로 파인튜닝해 CER/WER 개선을 비교할 예정입니다. 이 "표준 모델이 못하는 영역을 데이터로 개선"하는 측정이 프로젝트의 핵심 서사입니다. 이후 CTranslate2로 변환해 백엔드 `WHISPER_MODEL_DIR`에 연결합니다.



---

# Part C. 배운 것과 로드맵

## C-1. 개발하며 굳어진 관점

학습보다 먼저 파이프라인 뼈대를 관통시키면, 데이터가 준비되기 전에도 "돌아가는 데모"를 확보하고 이후 개선 전/후를 비교할 기준선을 얻습니다. 엔진 선택은 환경 제약에서 출발했습니다(ffmpeg 부재 → faster-whisper). 개발 환경 구축도 필요 이상으로 무겁게 가지 않고(커맨드라인 도구), 가정을 실제로 점검했습니다(WHPX 가속 확인 후 불필요한 관리자 단계 생략). 프레임워크 버전이 바뀌면 관례가 깨질 수 있어(SDK 57의 FormData 파일 파트 폐지) 버전 고정 문서를 먼저 확인하는 것이 필요했습니다.

## C-2. 면접에서 이야기할 핵심

아직 학습 단계 전이지만, 지금까지의 서사는 "표준 모델의 약점을 데이터로 개선한다"는 방법론을 위한 기반을 만든 과정입니다. 파이프라인을 STT와 변환 두 단계로 분리하고 각 자리를 교체 가능한 인터페이스로 둔 것, 서버 기반으로 앱을 얇게 유지한 것, 환경 제약에서 엔진을 고른 것이 그 예입니다. 다음 단계에서 AI Hub 방언 데이터로 Whisper를 파인튜닝하고 표준 대비 인식률 개선을 측정하면, ETRI 객체인식 경험(원인 분석 → 데이터 보강 → 재학습)과 방법론적으로 같은 선상에서 이야기할 수 있습니다.

## C-3. 앞으로의 로드맵

- [x] 기획 정리와 기술 스택 확정 (Part 0)
- [x] 백엔드 STT+변환 파이프라인 API 뼈대 (B-1)
- [x] 앱 녹음 → 전송 → 결과 뼈대 (B-2)
- [x] 개발용 안드로이드 에뮬레이터 환경 (B-3)
- [x] 업로드 경로 버그 해결 (B-4)
- [x] 디자인 방향 확정과 1차 구현 (B-5)
- [x] GitHub 공개 저장소 구성 (B-6)
- [x] AI Hub 방언 데이터 전처리 스크립트·테스트 (B-7)
- [x] 용량 대응 Colab 부분 다운로드 노트북 (B-8) — 실제 데이터 다운로드·실행은 사용자 몫
- [ ] Whisper 파인튜닝(표준 대비 인식률 개선 측정)과 CTranslate2 변환 연결 (B-9)
- [ ] 규칙 스텁 → KoBART/T5 사투리→표준어 변환 모델
- [ ] 첫 실행 온보딩 화면·정확한 Gowun Batang 폰트 등 디자인 심화
- [ ] 플레이스토어 배포(개발자 계정·개인정보처리방침·EAS Build)

---

문서 관리: 기능이나 방향이 바뀌면 이 파일도 함께 수정하고, 상단 "0. 업데이트 이력"에 한 줄 추가합니다.
